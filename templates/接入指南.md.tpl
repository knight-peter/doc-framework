# 接入指南（doc-framework）

> 本文档由 doc-framework 安装时生成到项目根。**仅执行一次**：AI 按本文档完成项目接入初始化后，**删除本文档**，并从 AGENTS.md 移除接入引导段（保留永久段）。
> 执行方式：开发者对 AI 说"初始化项目"（或"接入这套体系"），AI 读取本文档执行。

## 模式判定

| 项目状态 | 模式 | 流程 |
|----------|------|------|
| 已有代码 | 模式 A（默认，代码库探测） | 扫描代码库（含多应用探测）→ 档案草案（含应用清单）→ 确认 → 骨架 + 第一份契约 |
| 全新项目 | 模式 B（提问） | 回答提问（含"有哪些部署单元"）→ 档案 + 骨架 |

> **文档根命名（先确定）**：语言模式只决定根目录名与内部命名，二者一一对应——**中文 `doc-framework/`**（`项目档案.md`、`模块/`、`总契约.md`…）或 **英文 `doc-framework-en/`**（`profile.md`、`modules/`、`boundaries/`、`standards/`、`plans/`、`explore/`、`contract.md`…）；语言由根目录名识别，一个项目只建其中一个，且内部命名**要么全中文、要么全英文，禁止混用**。下文 `{文档根}` 即上述目录名。

## 模板来源（初始化必读）

doc-framework 的文档骨架必须**从官方模板渲染生成**，禁止凭空自创结构。模板位置按安装方式区分：

| 安装方式 | 模板路径 |
|----------|----------|
| 方式 A（npm/pnpm 依赖） | `node_modules/doc-framework/templates/` |
| 方式 B（install.sh 脚本） | 项目根无 node_modules 时，读取 `AGENTS.md` 中记录的模板来源标记（含仓库 URL + 版本），`git clone` 到临时目录后读取 `templates/`；临时目录用后即弃 |

> 所有模板以 `{占位符}` 标记待填内容，初始化时必须**全部渲染为项目实际值**，渲染规则见下节。

### 模板 → 产出映射

| 产出文档 | 来源模板 | 渲染要点 |
|----------|----------|----------|
| `{文档根}/项目档案.md` | `templates/profile.md.tpl` | **§应用清单（每应用：标识/类型/技术栈/代码根/规范文件/数据库/依赖）**、`{主键策略}` 等全局段 |
| `{文档根}/总契约.md` | `templates/总契约.md.tpl` | `{文档根}` `{业务域}` **§三 应用拓扑（按应用清单渲染）** `{模块名}` `{流程名}` |
| `{文档根}/测试规范.md` | `templates/测试规范.md.tpl` | `{文档根}` `{业务主表}` `{流程key}` `{流程引擎库}` |
| `{文档根}/接口规范.md` | `templates/接口规范.md.tpl` | `{认证方式}`（如 JWT） |
| `{文档根}/规范/类型-前端.md`、`规范/类型-后端.md` | `templates/规范/类型-前端.md.tpl`、`类型-后端.md.tpl` | 类型层共性（无前端/无后端应用时可不生成对应类型层） |
| `{文档根}/规范/应用-{应用标识}.md`（每应用一份） | `templates/规范/应用-{应用标识}.md.tpl` | `{应用标识}` `{类型}` `{代码根路径}` `{技术栈}` `{栈特有约定}` `{覆盖项}` `{标杆模块}` |
| `{文档根}/边界/{能力}边界.md` | `templates/边界/{能力}边界.md.tpl` | 按需创建（仅多模块共享能力时），`{能力}` 如 `工作流` `附件` `权限` `服务间调用`；`{模块名}` `{规则}` |
| `{文档根}/计划/YYYY-MM-DD-{主题}实施.md` | `templates/计划/YYYY-MM-DD-{实施主题}.md.tpl` | 跨模块计划用（引用多个契约）；单模块计划落盘 `{文档根}/模块/{模块名}/计划/`，均由 /module-plan 生成；头部标**计划形态**（`完整` / `轻量`——轻量只保留 逐应用表态/变更文件清单/任务清单 三节）；含**勾选式任务清单**（进度事实源）与逐应用表态（= 授权作用域）；归档目录 `计划/archive/` 由 /module-plan 维护 |
| `{文档根}/模块/{模块名}/{契约|接口|测试|test.sh}` | `templates/模块/{模块名}/` 下四个模板 | 首个标杆模块从模板生成，`{模块名}` `{业务域}` `{主责服务}` `{应用落点}` `{表名}` 等；契约 **§8.2 关键验收场景**用 Scenario 写法（`#### Scenario: SC-n` + WHEN/THEN），测试.md 用例标注对应场景编号 |
| `{文档根}/探索/YYYY-MM-DD-{主题}.md` | `templates/探索/YYYY-MM-DD-{主题}.md.tpl` | 按需产生（仅重大跨模块/跨应用决策时，由 /module-explore 落盘）；单次决策记录，不参与 check 硬校验 |

### 渲染纪律

1. **逐占位符替换**：渲染后全文扫描，不得残留任何 `{...}` 占位符（可用 `grep -n '{' {产出文件}` 自检）；
2. **技术栈专属细节按项目实际填写**：模板中带"（{如 ...}）"的示例仅为提示，替换为项目真实技术栈；
3. **引用不复制**：项目已有原始规范（如 `{文档根}/standards/`）时，规范文档在模板结构内**引用**原始规范路径，不复制全文，避免双维护；三层规范（全局/类型/应用）同样逐层引用、禁止复制；
4. **路径一律从应用清单派生**：skill 与文档中的 glob、代码引用不得写死某个目录名（如 `apps/`），一律以应用清单登记的代码根为准；
5. **完成后运行校验**：`npx doc-framework check`（若可用）验证骨架完整性与占位符残留。

## 模式 A：旧项目接入（代码库探测）

开发者对代码了解不深也无需回答技术问题，AI 自己读代码：

1. **扫描代码库自动识别**：
   - **多应用探测（先做这个）**：扫描全部构建信号——`pom.xml` / `build.gradle`（Java 服务）、`go.mod` / `requirements.txt` / 含 nest|express 的 `package.json`（其他后端/BFF）、`package.json` + `vite.config.*` + `vue-router`（web 端）、`manifest.json` + `pages.json`（uni-app 移动端）、`app.json` + `project.config.json`（小程序）。**出现多处即多应用**，逐个推断：类型（前端端/后端服务/聚合层/共享库）、技术栈、代码根（**按现状登记任意路径**，不要求规整目录）、数据库/存储（从连接配置反推）、应用间依赖（从聚合层调用代码/网关路由反推）；
   - 主键 / 软删除 / 审计字段约定（从 DDL 与实体反推，注意**不同服务可能不同**——记为全局默认 + 应用覆盖）；
   - 权限体系（注解 / 前端指令 / 菜单表）；
   - 已有模块清单（controller / views / pages 目录）；
   - 工作流引擎依赖；
   - 模块形态（CRUD / 审批流等代码模式）。
2. **生成档案草案**（对照 `profile.md.tpl`），**应用清单逐行标注证据来源与置信度**：确定项引用具体文件路径，推测项标注"待确认"；
3. **仅对代码中看不出来的信息提问**：业务域划分、文档语言（中文/英文）、团队特有约定（如命名风格偏好）；应用归属推测项一并确认；
4. **提炼第一份模块契约草案**：从已识别的最完整模块提炼（对照模块契约模板，图用 mermaid 绘制），作为后续标杆——**先确定主责服务归属**（探测不到就问一次，写进档案），§5 应用落点表按探测结果填行；**跨模块业务流程同步登记到总契约 §六**（首个 BF 编号）；
5. **生成产出**（**全部从模板渲染**，见「模板来源」）：
   - 档案：`{文档根}/项目档案.md`（对照 `profile.md.tpl` 逐项填写，**应用清单是唯一需人工确认的部分**）；
   - 文档骨架：总契约（业务域 §二、**应用拓扑 §三**、模块索引 §四、依赖矩阵 + 全景图 §五、跨模块业务流程 §六）、测试规范、接口规范、规范三层（`规范/类型-前端.md`、`规范/类型-后端.md`、**每个应用一份 `规范/应用-{应用标识}.md`**）、`模块/`（含 `模块/{模块名}/计划/` 单模块计划子目录，按需创建）、`边界/`、`计划/`（跨模块计划目录）、`探索/`（探索记录目录，按需产生）及初始文档；
   - 校验 `AGENTS.md`（安装时已写入引导段 + 永久段，此处校正永久段内容）；
   - **自检**：渲染后全文扫描无 `{占位符}` 残留；运行 `npx doc-framework check` 验证（若可用）。
6. **清理**：
   - 从 `AGENTS.md` 移除**接入引导段**（保留永久段：文档路径约定、实施文档日期命名、skill 触发命令）；
   - 删除项目根 `接入指南.md`；
   - 删除 `{文档根}/README.md`（安装脚本预置的骨架引导文件）。

## 模式 B：全新项目（提问）

没有代码可探测，只能提问：

1. 提问：**本项目有哪些部署单元/代码单元（web 端、移动端、各后端服务、聚合层…）？各自的类型与技术栈？** 目录约定、主键 / 软删除 / 权限体系、业务域、文档语言；
2. 生成档案（含应用清单）+ 骨架（同样**从模板渲染**，见「模板来源」）；第一个模块契约从需求写起（契约 = 需求分析，先于代码）；
3. 同样执行模式 A 的第 5 步（校验 AGENTS.md）与第 6 步（清理）。

## 完成标准

- [ ] 项目档案已生成并经开发者确认（从 `profile.md.tpl` 渲染，**应用清单逐行确认**，无占位符残留）
- [ ] 文档骨架已生成（总契约〔业务域 + **应用拓扑** + 依赖矩阵/全景图 + 跨模块业务流程〕 / 测试规范 / 接口规范 / **规范三层：类型-前端、类型-后端、应用-{应用标识}（每应用一份）** / 模块 / 边界 / 计划）
- [ ] 骨架文档均从 `templates/` 对应模板渲染，**无 `{占位符}` 残留**
- [ ] 第一份模块契约已提炼（标杆，含**主责服务声明 + §5 应用落点表**，从模块模板渲染）
- [ ] AGENTS.md 接入引导段已移除（永久段保留）
- [ ] 项目根《接入指南.md》已删除
- [ ] 安装预置的 `{文档根}/README.md`（骨架引导）已删除
- [ ] （若可用）`npx doc-framework check` 通过

完成后即可开始日常开发（需求模糊时先探索）：`/module-explore {主题}` → `/module-doc {模块名}`（三模式自动探测 + 编号确认）→ `/module-plan {模块名}` → `/module-code {模块名}` → `/module-review {模块名}`。

## 触发词说明（DSH）

DSH 的显式触发只匹配英文技能名（`/技能名` 手势仅支持小写字母/数字/连字符），实际触发词（记忆点：探路、写文档、写计划、写代码、收尾检查）：

| 命令 | 对应技能 |
|------|----------|
| `/module-explore {主题}` | 探索与讨论技能（写契约前想清楚：调研现状、方案对比、有证据的推荐；不写契约/计划/代码，默认不落盘） |
| `/module-doc {模块名}` | 模块文档技能（**零后缀**；三模式自动探测：A 需求驱动 / B 代码对账 / C 产物补齐；确认门带编号可强制切换；无契约+有代码+带需求走 B→A 复合） |
| `/module-plan {模块名}` | 实施计划技能（计划创建/修订/状态流转/归档，含逐应用表态与任务清单） |
| `/module-code {计划路径}` | 模块代码技能（只执行已批准的实施计划，白名单为边界，逐项勾选任务，完成后回写） |
| `/module-review {模块名}` | 模块审查技能（分级问题清单 + 多应用对账 + 完成度对账 + 回写查账） |

也可用自然语言描述（如"用模块代码技能…"），AI 会按技能目录自行加载对应技能。其他 agent（Claude Code 等）按各自技能触发约定使用。
