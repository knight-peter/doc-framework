# 接入指南（doc-framework）

> 本文档由 doc-framework 安装时生成到项目根。**仅执行一次**：AI 按本文档完成项目接入初始化后，**删除本文档**，并从 AGENTS.md 移除接入引导段（保留永久段）。
> 执行方式：开发者对 AI 说"初始化项目"（或"接入这套体系"），AI 读取本文档执行。

## 接入模式判定

> 本节是**接入初始化**的两种方式，与 `/module-doc` 的文档模式（M 合并 / B 代码对账 / C 产物补齐）**不是一回事**，勿混。

| 项目状态 | 接入方式 | 流程 |
|----------|----------|------|
| 已有代码 | 代码库探测（默认） | 扫描代码库（含多应用探测）→ 档案草案（含应用清单）→ 确认 → 骨架 + 第一份契约 |
| 全新项目 | 提问生成 | 回答提问（含"有哪些部署单元"）→ 档案 + 骨架 |

> **文档根与命名（固定）**：文档根固定 `doc-framework/`，目录 / 文件 / 章节 / 头部字段 / 枚举值**一律中文**（完整清单见下「模板 → 产出映射」与框架仓库 `docs/设计文档.md` §8.1）。**v2.6.0 起不再支持英文产物命名**——英文只影响**文档内容**，不影响命名；检出 `doc-framework-en/` 或历史名 `docs-framework/` 时 `check` 会显式报错并给迁移方向。

## 模板来源（初始化必读）

doc-framework 的文档骨架必须**从官方模板渲染生成**，禁止凭空自创结构。模板位置按安装方式区分：

| 安装方式 | 模板路径 |
|----------|----------|
| 方式 A（npm/pnpm 依赖） | `node_modules/doc-framework/templates/` |
| 方式 B（install.sh 脚本） | 项目根无 node_modules 时，读取 `AGENTS.md` 中记录的模板来源标记（含仓库 URL + 版本），`git clone` 到临时目录后读取 `templates/`；临时目录用后即弃 |

> 模板里**三类记法各不相同**（v2.6.0 起固定，详见下节「渲染纪律」第 1 条）：**`{{字段}}`＝待填内容**（必须全部渲染为项目实际值，渲染后不得残留）；**`{记法}`＝路径 / 命名记法**（如 `{文档根}`、`{模块名}`——不是待填字段，落值或照原样保留，不要当模板 bug 删掉）；**`【…】`＝示例 / 可选项**（填好后连方括号一起删除）。判断依据：**上表「渲染要点」列出的 token 是字段名**，按 `{{…}}` 在模板里定位对应待填字段。


### 模板 → 产出映射

| 产出文档 | 来源模板 | 渲染要点（**待填字段 `{{…}}` 全清单**） |
|----------|----------|----------|
| `{文档根}/项目档案.md` | `templates/项目档案.md.tpl` | **§应用清单（每应用：标识/类型/技术栈/代码根/规范文件/数据库/依赖）**；其余：`{app-web}` `{app-mobile}` `{svc-main}` `{svc-bff}` `{代码根路径}`（清单每行各填一次）`{工作流引擎，可选}` `{主键策略}` `{软删除约定}` `{审计字段}` `{域清单见 总契约.md §2}` `{分页约定}` `{形态1}` `{形态2}` `{应用标识}` `{标杆模块}` `{代码路径}` `{文档根}` |
| `{文档根}/总契约.md` | `templates/总契约.md.tpl` | `{文档根}` `{业务域}` `{一句话说明}` **§3 应用拓扑（按应用清单渲染）**`{app-web}` `{svc-bff}` `{svc-main}` `{代码根}` **§4/§5 索引与矩阵**`{模块名}` `{形态}` `{标杆}` `{边界文档}` `{契约路径}` `{依赖}` `{被谁依赖}` **§6 业务流程**`{流程名}` `{模块A}` `{模块B}` `{依赖说明}` `{涉及模块}` `{涉及应用}` `{触发/入口}` `{角色/模块（应用）}` `{动作}` `{模块（应用）}` `{svc-main: 路径; app-web: 路径; app-mobile: 路径}`（§4 各应用落点列）；正文示例里的 `{应用标识}` `{域}` `{实体}` `{操作}` 与 `{主题}` `{能力}` `{计划路径}` 是**说明性记法，随示例一并改写或删除，不要照抄** |
| `{文档根}/测试规范.md` | `templates/测试规范.md.tpl` | `{文档根}` `{模块名}` `{业务主表}` `{业务主数据}` `{业务主表/关联表}` `{流程key}` `{流程引擎库}` `{过滤字段}` `{过滤值}` `{服务}` |
| `{文档根}/接口规范.md` | `templates/接口规范.md.tpl` | `{认证方式}`（如 JWT）`{权限校验模型}` `{域}` `{实体}` `{操作}` `{资源}` `{动作}` `{token}` `{id}` `{ids}` |
| `{文档根}/规范/类型-前端.md`、`规范/类型-后端.md` | `templates/规范/类型-前端.md.tpl`、`类型-后端.md.tpl` | 类型层共性（无前端/无后端应用时可不生成对应类型层）；`{文档根}` `{标杆模块1}` `{代码路径}` `{参考点}` `{应用标识}` `{域}` `{实体}` `{操作}`（前端另有 `{后端}`=后端上传接口前缀）；后端另有 `{代码根}` `{模块}` `{说明}` `{权限校验模型}` `{Xxx}` `{XxxEntity}` |
| `{文档根}/规范/应用-{应用标识}.md`（每应用一份） | `templates/规范/应用-{应用标识}.md.tpl` | `{应用名称}` `{应用标识}` `{前端端 / 后端服务 / 聚合层(BFF) / 共享库}`（=「类型」）`{代码根路径}` `{技术栈}` `{服务端应用填写，如 mysql: order_db；前端端填 —}` `{调用方向，如 svc-bff → svc-order；与档案应用清单一致}` `{应用内结构}` `{服务端应用填，如 DDL、实体、Mapper、Service、Controller 等 N 件套}` `{栈特有约定}` `{权限控制方式}` `{路由机制}` `{请求封装}` `{组件库与列表页模式}` `{依赖注入/注解风格}` `{覆盖项}` `{全局默认}` `{本应用}` `{理由}`（第 4 节覆盖表，**无覆盖时整行删除**；`{雪花ID}` `{AUTO_INCREMENT}` `{历史库遗留}` 是该行的示例取值，替换或删除）`{标杆模块}` `{代码路径}` `{参考点}` `{技术栈特有检查项}`。**注意**：模板里 `{如 Vue 3 / Element Plus …}`、`{如：主键策略}`、`{...}`、`{层级}` 这类**示例/说明性花括号不是待填字段**——按上表替换后，示例行要么改成项目实际值，要么按第 4 节说明整行删除，**不得原样留下** |
| `{文档根}/边界/{能力}边界.md` | `templates/边界/{能力}边界.md.tpl` | 按需创建（仅多模块共享能力时），`{能力}` 如 `工作流` `附件` `权限` `服务间调用`；`{模块名}` `{规则}` |
| `{文档根}/计划/YYYY-MM-DD-{主题}.md` | `templates/计划/YYYY-MM-DD-{实施主题}.md.tpl` | 跨模块计划用（引用多个契约，或指向未建档模块的首次建模计划）；单模块计划落盘 `{文档根}/模块/{模块名}/计划/`，均由 /module-plan 生成；头部标**计划形态**（`完整` / `轻量`——轻量只保留 逐应用表态/变更文件清单/任务清单 三节、**不含语义增量**）；完整计划含**「语义增量」节**（ADDED/MODIFIED/REMOVED + `合并状态`，位置＝逐应用表态之后、变更文件清单之前）与 `> 依据探索记录：`（待实现模块必填）；含**勾选式任务清单**（进度事实源）与逐应用表态（= 授权作用域）；归档目录 `计划/归档/` 由 /module-plan 维护。**待填占位符全清单**：头部 `{文档根}` `{模块名}` `{标杆模块}` `{代码路径}`；§0 修订记录 `{YYYY-MM-DD}`；§1 `{需求背景与目标描述}`；§2 `{svc-main}` `{svc-bff}` `{app-web}` `{app-mobile}`（按档案应用清单逐行替换）；增量三表 `{x.y}` `{表/章节}` `{对象}` `{目标语义}` `{现值}` `{目标值}` `{理由}`；建模补充（仅首次建模）`{本模块管什么、不管什么；与相邻模块的边界}` `{初始状态}` `{下一状态}` `{调用谁 / 被谁调用 / 共享的状态机或权限域；跨模块接口的字段与语义（此时该模块尚无契约，接口语义以此为准）}` `{场景名}` `{前置}` `{动作}` `{期望}`；§3 清单 `{服务代码根}` `{应用标识}` `{文件路径}` `{说明}`；§4 任务 `{任务}`；§6 决策 `{决策 1}` `{决策 2}`；§8 验收 `{验收项}`；§9 `{待确认项}`。任务行与「兼容性声明」等处的 `{任务：如 …}`、`{兼容（新增字段/新增接口） ｜ …}` 是**示例文案**，按项目改写或删除 |
| `{文档根}/模块/{模块名}/{契约｜接口}[, 测试｜test.sh]`（测试为可选） | `templates/模块/{模块名}/` 下四个模板 | 首个标杆模块从模板生成（**旧项目**：从代码提炼；**全新项目**：零代码不预建，实施后由 `/module-doc` M 的 `create` 分支据增量 + 建模补充建档），`{模块名}` `{业务域}` `{主责服务}` `{应用落点}` `{表名}` 等；**`test.sh` 模板另有 `{流程key}` `{接口基础路径}`**（后者＝接口路径前缀，如 `order`；无工作流 / 单服务模块按模板注释删减）；契约 **§8.2 关键验收场景**用 Scenario 写法（`#### Scenario: SC-n` + WHEN/THEN），测试.md 用例标注对应场景编号 |
| `{文档根}/探索/YYYY-MM-DD-{主题}.md` | `templates/探索/YYYY-MM-DD-{主题}.md.tpl` | 由 /module-explore 落盘：重大跨模块/跨应用决策**按需**，**待实现模块场景强制**（额外含第 5 节「待实现模块清单与关系」，是首次建模计划的唯一跨会话输入）；单次决策记录，不参与 check 硬校验 |

### 渲染纪律

1. **三类记法，各有各的处理**（v2.6.0 起语法固定，机器可验）：

| 记法 | 含义 | 渲染时怎么做 |
|---|---|---|
| `{{字段}}`（**双花括号**） | **待填字段** | **必须**替换为项目实际值；渲染后**不得残留任何双花括号** |
| `{记法}`（**单花括号**） | 路径 / 命名记法（`{文档根}`、`{模块名}`、`{域}:{实体}:{操作}`） | **不是待填字段**：落到项目实际值（如 `{文档根}` → `doc-framework`）或照原样保留，**不要**当成模板 bug 删掉 |
| `【…】`（方括号） | 示例 / 可选项 | 按项目实际改写；**填好后连方括号一起删除**，不要留在产物里 |

   自检：`grep -n '{{' <产出文件>`（**应为空**）。
   **特例 `{{框架版本}}`**：四份全局文档与计划模板首行的结构版本哨兵写的是 `{{框架版本}}`——渲染时**替换为框架仓库 `package.json` 的 version**（如 `v2.6.0`）。这样做是为了让版本号只有 package.json 一个来源（此前 5 份模板各自硬编码，发版要手改 6 处）；填上真实版本后 `check` 的 R4 才能比较"文档结构版本 vs 框架版本"。**若留着不填也不会报错**（哨兵按"随框架版本"处理），但填上更利于后续升级对账。
`check` 会把残留的 `{{…}}` 报成「占位符未渲染」并**使退出码为 1**——所以"渲染干净"是可机检的，不靠人眼；
2. **技术栈专属细节按项目实际填写**：模板中方括号内的示例仅为提示，替换为项目真实技术栈；
3. **引用不复制**：项目已有原始规范（如 `{文档根}/standards/`）时，规范文档在模板结构内**引用**原始规范路径，不复制全文，避免双维护；三层规范（全局/类型/应用）同样逐层引用、禁止复制；
4. **路径一律从应用清单派生**：skill 与文档中的 glob、代码引用不得写死某个目录名（如 `apps/`），一律以应用清单登记的代码根为准；
5. **完成后运行校验**：`npx doc-framework check`（若可用）验证骨架完整性与占位符残留。

## 旧项目接入（代码库探测）

开发者对代码了解不深也无需回答技术问题，AI 自己读代码：

1. **扫描代码库自动识别**：
   - **多应用探测（先做这个）**：扫描全部构建信号——`pom.xml` / `build.gradle`（Java 服务）、`go.mod` / `requirements.txt` / 含 nest|express 的 `package.json`（其他后端/BFF）、`package.json` + `vite.config.*` + `vue-router`（web 端）、`manifest.json` + `pages.json`（uni-app 移动端）、`app.json` + `project.config.json`（小程序）。**出现多处即多应用**，逐个推断：类型（前端端/后端服务/聚合层/共享库）、技术栈、代码根（**按现状登记任意路径**，不要求规整目录）、数据库/存储（从连接配置反推）、应用间依赖（从聚合层调用代码/网关路由反推）；
   - 主键 / 软删除 / 审计字段约定（从 DDL 与实体反推，注意**不同服务可能不同**——记为全局默认 + 应用覆盖）；
   - 权限体系（注解 / 前端指令 / 菜单表）；
   - 已有模块清单（controller / views / pages 目录）；
   - 工作流引擎依赖；
   - 模块形态（CRUD / 审批流等代码模式）。
2. **生成档案草案**（对照 `项目档案.md.tpl`），**应用清单逐行标注证据来源与置信度**：确定项引用具体文件路径，推测项标注"待确认"；
3. **仅对代码中看不出来的信息提问**：业务域划分、文档语言（中文/英文）、团队特有约定（如命名风格偏好）；应用归属推测项一并确认；
4. **提炼第一份模块契约草案**：从已识别的最完整模块提炼（对照模块契约模板，图用 mermaid 绘制），作为后续标杆——**先确定主责服务归属**（探测不到就问一次，写进档案），§5 应用落点表按探测结果填行；**跨模块业务流程同步登记到总契约 §6**（首个 BF 编号）；
5. **生成产出**（**全部从模板渲染**，见「模板来源」）：
   - 档案：`{文档根}/项目档案.md`（对照 `项目档案.md.tpl` 逐项填写，**应用清单是唯一需人工确认的部分**）；
   - 文档骨架：总契约（业务域 §2、**应用拓扑 §3**、模块索引 §4、依赖矩阵 + 全景图 §5、跨模块业务流程 §6）、测试规范、接口规范、规范三层（`规范/类型-前端.md`、`规范/类型-后端.md`、**每个应用一份 `规范/应用-{应用标识}.md`**）、`模块/`（含 `模块/{模块名}/计划/` 单模块计划子目录，按需创建）、`边界/`、`计划/`（跨模块计划目录）、`探索/`（探索记录目录，按需产生）及初始文档；
   - 校验 `AGENTS.md`（安装时已写入引导段 + 永久段，此处校正永久段内容）；
   - **自检**：渲染后全文扫描无 `{占位符}` 残留；运行 `npx doc-framework check` 验证（若可用）。
6. **清理**：
   - 从 `AGENTS.md` 移除**接入引导段**（保留永久段：文档路径约定、实施文档日期命名、skill 触发命令）；
   - 删除项目根 `接入指南.md`；
   - 删除 `{文档根}/README.md`（安装脚本预置的骨架引导文件）。

## 全新项目接入（提问生成）

没有代码可探测，只能提问：

1. 提问：**本项目有哪些部署单元/代码单元（web 端、移动端、各后端服务、聚合层…）？各自的类型与技术栈？** 目录约定、主键 / 软删除 / 权限体系、业务域、文档语言；
2. 生成档案（含应用清单）+ 骨架（同样**从模板渲染**，见「模板来源」）；**契约 = 当前代码的语义映射——全新项目此刻零代码，因此不预先建档**：首个模块走 `/module-explore`（强制落盘）→ `/module-plan`（首次建模计划：语义写进「语义增量」节）→ 实施 → `/module-doc` M 的 `create` 分支建档 + 回写总契约；
3. 同样执行「旧项目接入（代码库探测）」的第 5 步（校验 AGENTS.md）与第 6 步（清理）。

## 完成标准

- [ ] 项目档案已生成并经开发者确认（从 `项目档案.md.tpl` 渲染，**应用清单逐行确认**，无占位符残留）
- [ ] 文档骨架已生成（总契约〔业务域 + **应用拓扑** + 依赖矩阵/全景图 + 跨模块业务流程〕 / 测试规范 / 接口规范 / **规范三层：类型-前端、类型-后端、应用-{应用标识}（每应用一份）** / 模块 / 边界 / 计划）
- [ ] 骨架文档均从 `templates/` 对应模板渲染，**无 `{占位符}` 残留**
- [ ] 第一份模块契约已就位——**旧项目**：从代码提炼（标杆，含**主责服务声明 + §5 应用落点表**，从模块模板渲染）；**全新项目**：此刻零代码不预建，首个模块实施后由 `/module-doc` M 的 `create` 分支建档
- [ ] AGENTS.md 接入引导段已移除（永久段保留）
- [ ] 项目根《接入指南.md》已删除
- [ ] 安装预置的 `{文档根}/README.md`（骨架引导）已删除
- [ ] （若可用）`npx doc-framework check` 通过

完成后即可开始日常开发（需求模糊时先探索）：`/module-explore {主题}` → `/module-plan {模块名}`（语义写进「语义增量」节；待实现模块 = 首次建模计划）→ 批准 → `/module-code {计划路径}` → `/module-doc {模块名}`（M 合并增量落账）→ `/module-review {模块名}`。

## 触发词说明（DSH）

DSH 的显式触发只匹配英文技能名（`/技能名` 手势仅支持小写字母/数字/连字符），实际触发词（记忆点：探路、写文档、写计划、写代码、收尾检查）：

| 命令 | 对应技能 |
|------|----------|
| `/module-explore {主题}` | 探索与讨论技能（写契约前想清楚：调研现状、方案对比、有证据的推荐；不写契约/计划/代码，默认不落盘） |
| `/module-doc {模块名}` | 模块文档技能（**零后缀**；三模式自动探测：M 合并〔create 待实现模块首次建档 / update 存量落账〕 / B 代码对账 / C 产物补齐；确认门带编号可强制切换；**无契约 + 无代码（待实现模块）→ 重定向 `/module-plan` 首次建模计划**） |
| `/module-plan {模块名}` | 实施计划技能（计划创建/修订/状态流转/**归档**——归档时校验契约 §9 已登记、写入提交 SHA、移入 `计划/归档/`（不自动删除）；含逐应用表态与任务清单） |
| `/module-code {计划路径}` | 模块代码技能（只执行已批准的实施计划，白名单为边界，逐项勾选任务，完成后回写） |
| `/module-review {模块名}` | 模块审查技能（分级问题清单 + 多应用对账 + 完成度对账 + 回写查账） |

也可用自然语言描述（如"用模块代码技能…"），AI 会按技能目录自行加载对应技能。其他 agent（Claude Code 等）按各自技能触发约定使用。
