# 接入指南（doc-framework）

> 本文档由 doc-framework 安装时生成到项目根。**仅执行一次**：AI 按本文档完成项目接入初始化后，**删除本文档**，并从 AGENTS.md 移除接入引导段（保留永久段）。
> 执行方式：开发者对 AI 说"初始化项目"（或"接入这套体系"），AI 读取本文档执行。

## 模式判定

| 项目状态 | 模式 | 流程 |
|----------|------|------|
| 已有代码 | 模式 A（默认，代码库探测） | 扫描代码库 → 档案草案 → 确认 → 骨架 + 第一份契约 |
| 全新项目 | 模式 B（提问） | 回答提问 → 档案 + 骨架 |

## 模式 A：旧项目接入（代码库探测）

开发者对代码了解不深也无需回答技术问题，AI 自己读代码：

1. **扫描代码库自动识别**：
   - 技术栈（`pom.xml` / `package.json` / `build.gradle`）；
   - 目录分层（后端包结构、前端 views/api 结构）；
   - 数据库连接与 SQL 脚本；
   - 主键 / 软删除 / 审计字段约定（从 DDL 与实体反推）；
   - 权限体系（注解 / 前端指令 / 菜单表）；
   - 已有模块清单（controller / views 目录）；
   - 工作流引擎依赖；
   - 模块形态（CRUD / 审批流等代码模式）。
2. **生成档案草案**（对照 `profile.md.tpl`），逐项标注**证据来源与置信度**：确定项引用具体文件路径，推测项标注"待确认"；
3. **仅对代码中看不出来的信息提问**：业务域划分、文档语言（中文/英文）、团队特有约定（如命名风格偏好）；
4. **提炼第一份模块契约草案**：从已识别的最完整模块提炼（对照模块契约模板，图用 mermaid 绘制），作为后续标杆；**跨模块业务流程同步登记到总契约 §五**（首个 BF 编号）；
5. **生成产出**：
   - 档案：`doc/项目档案.md`（中文）/ `docs/profile.md`（英文）；
   - 文档骨架：总契约（业务域 §二、模块索引 §三、依赖矩阵 + 全景图 §四、跨模块业务流程 §五）、测试规范、接口规范、`模块/`、`边界/`、`规范/`、`计划/` 目录及初始文档；
   - 校验 `AGENTS.md`（安装时已写入引导段 + 永久段，此处校正永久段内容）。
6. **清理**：
   - 从 `AGENTS.md` 移除**接入引导段**（保留永久段：文档路径约定、实施文档日期命名、skill 触发命令）；
   - 删除项目根 `接入指南.md`。

## 模式 B：全新项目（提问）

没有代码可探测，只能提问：

1. 提问：技术栈、目录约定、主键 / 软删除 / 权限体系、业务域、文档语言；
2. 生成档案 + 骨架；第一个模块契约从需求写起（契约 = 需求分析，先于代码）；
3. 同样执行模式 A 的第 5 步（校验 AGENTS.md）与第 6 步（清理）。

## 完成标准

- [ ] 项目档案已生成并经开发者确认
- [ ] 文档骨架已生成（总契约〔业务域 + 依赖矩阵/全景图 + 跨模块业务流程〕 / 测试规范 / 接口规范 / 模块 / 边界 / 规范 / 计划）
- [ ] 第一份模块契约已提炼（标杆）
- [ ] AGENTS.md 接入引导段已移除（永久段保留）
- [ ] 项目根《接入指南.md》已删除

完成后即可开始日常开发：`/module-doc {模块名}` → `/module-code {模块名}` → `/module-review {模块名}`。

## 触发词说明（DSH）

DSH 的显式触发只匹配英文技能名（`/技能名` 手势仅支持小写字母/数字/连字符），实际触发词（记忆点：写文档、写代码、收尾检查）：

| 命令 | 对应技能 |
|------|----------|
| `/module-doc {模块名}` | 模块文档技能（四件套创建/逆向提炼/更新） |
| `/module-code {模块名}` | 模块代码技能（按实施计划实施代码） |
| `/module-review {模块名}` | 模块审查技能（分级问题清单 + 回写查账） |

也可用自然语言描述（如"用模块代码技能…"），AI 会按技能目录自行加载对应技能。其他 agent（Claude Code 等）按各自技能触发约定使用。
