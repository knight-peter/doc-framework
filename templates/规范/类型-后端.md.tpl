# 后端开发规范（类型层）

> 定位：**所有后端类应用（业务服务 / 聚合层 BFF…）共享的共性约定**。
> 分层纪律：本文件只写跨后端共性；各服务的栈细节（框架注解风格、主键/软删除覆盖、目录结构、每模块文件清单）一律写在 `规范/应用-{应用标识}.md`，**本文件不复制应用层内容，应用层不复制本文件内容**（防双维护）。
> **渲染义务（初始化时）**：本文件的双花括号字段必须替换为项目实际值，替换后不得残留任何双花括号。`{…}` 是路径与命名记法（如 `{模块名}`、`{域}:{实体}:{操作}`），照原样保留。

## 0. 引用与优先级（唯一入口，防双维护）

- **本文件**：后端类型层共性（速查索引），`/module-doc` 导航区与 `/module-review` 按此引用；
- **应用层**：`规范/应用-{应用标识}.md`（每个后端服务一份，承载栈细节与覆盖项）；
- **全局层**：`{文档根}/接口规范.md`（认证/错误码/分页/接口兼容性，所有应用一致）；
- **冲突判定**：全局层 > 类型层（本文件） > 应用层（应用层只可细化，不得推翻上层）；
- **纪律**：逐层引用、禁止复制全文（避免双维护漂移）。

## 1. 分层与目录（原则）

- 分层结构：Controller → Service → 数据访问层 → 数据库（聚合层无数据访问层，只做编排/透传）；
- 目录约定：按档案「应用清单」中该服务的代码根；服务内包结构、每模块文件清单见对应 `规范/应用-{应用标识}.md`；
- SQL 变更脚本：`{代码根}/sql/change/YYYY-MM-DD-{模块}_{说明}.sql`（按服务各自的目录，不修改历史建表脚本）；**需人工在生产环境执行的脚本必须在模块契约 §10 登记**（防上线遗漏，正文不复制进契约）。

## 2. 命名

- 类/文件：PascalCase（如 `{{实体类名}}`、`I{{实体类名}}Service`、`{{实体类名}}Mapper`）；
- 接口权限标识：`{域}:{实体}:{操作}`（`list/query/add/edit/remove/export/view`，**跨应用不变量**）；
- 数据库表：snake_case；字段：snake_case；主键：`{实体}_id`；
- 枚举类：`XxxStatusEnum` / `XxxAuditResultEnum`（状态/结果等），持久化 code 值。

## 3. 权限体系（共性部分）

- 后端：接口级权限注解/拦截全覆盖（具体注解风格见应用层）；
- 写操作校验：{{写操作校验模型}}（如 岗位优先 + 单位/关系兜底，按项目实际；详见模块契约 §4.3）；
- 列表数据权限：按用户/单位/上级单位过滤（详见模块契约与档案通用约定）。

## 4. 通用约定（后端共性：只写"怎么用"，字段名与取值见档案）

> **字段名与取值的唯一真相在 `项目档案.md` 的「通用约定」**——本节只写后端在使用这些字段时的共性纪律，**不复制主键/审计字段/分页参数的取值**（复制即双维护）。

- 主键：按档案约定生成（服务可覆盖，覆盖必须在应用层规范显式声明并注明理由）；
- 软删除：逻辑删除列按档案约定，**查询/更新 SQL 必须携带软删除条件**；
- 审计字段：按档案约定在创建/更新时自动填充；
- 乐观锁：按档案约定携带版本号（更新时版本自增，WHERE 携带原版本，0 行影响抛并发冲突）；
- 分页：按档案约定的请求参数与响应形状（接口层形态见 `{文档根}/接口规范.md`）；
- 事务：多表写操作必须事务；
- 响应：统一响应结构（见 `{文档根}/接口规范.md`），错误文案可直接展示给用户，不暴露堆栈；
- 服务间调用：跨服务一律走接口（HTTP/MQ/RPC），**禁止跨服务直连数据库**；超时/重试/熔断等统一规则见 `边界/服务间调用边界.md`（如已建立）。

## 5. 标杆模板（类型级）

- {{标杆模块1}}（{{应用标识}}）：{{代码路径}}（{{参考点}}）。
