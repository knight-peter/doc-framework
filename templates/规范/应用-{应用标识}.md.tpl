# {应用名称} 开发规范（应用层）

> 应用标识：`{应用标识}`（与档案「应用清单」一致）
> 类型：{前端端 / 后端服务 / 聚合层(BFF) / 共享库}
> 代码根：`{代码根路径}`
> 定位：本应用**独有**的技术栈细节与目录约定。共性规则一律引用上层（`接口规范.md` → `规范/类型-前端.md` 或 `规范/类型-后端.md`），**本文件只写差异与覆盖项，禁止复制上层内容**（防双维护）。
> **渲染义务（初始化时）**：本文件所有 `{占位符}` 必须替换为项目实际值，替换后不得残留任何 `{...}`。

## 一、技术栈

- 技术栈（**必填渲染**）：{如 Vue 3 / Element Plus / Pinia / Vite；或 uni-app / uview-plus；或 Java 17 / Spring Boot 3 / MyBatis-Plus；或 Node / NestJS}；
- 数据库/存储：{服务端应用填写，如 mysql: order_db；前端端填 —}；
- 依赖的应用：{调用方向，如 svc-bff → svc-order；与档案应用清单一致}。

## 二、目录结构

- 代码根：`{代码根路径}`（档案应用清单登记值，禁止与其他应用代码根嵌套）；
- 应用内结构：{如 views/{层级}/{模块}/ + components/ + api/{业务域}/；或 pages/{模块}/ + api/；或 {模块}/controller|service|mapper}；
- 每模块文件清单：{服务端应用填，如 DDL、实体、Mapper、Service、Controller 等 N 件套}。

## 三、栈特有约定

{按应用实际填写，例如：}
- 权限控制方式：{如 `v-hasPermi="['{域}:{实体}:{操作}']"` 指令；或自研 `permi()` 函数（uni-app 无指令）}；
- 路由机制：{如 vue-router 动态路由 + `sys_menu.route_name` 一致；或 uni-app `pages.json` 注册}；
- 请求封装：{如 axios 实例 + 拦截器；或 uni.request 封装}；
- 组件库与列表页模式：{如 Element Plus 搜索/表格/分页组件族；或卡片流 + 一步一屏}；
- 依赖注入/注解风格：{如构造器注入 `@RequiredArgsConstructor`，禁止字段注入}。

## 四、全局默认的覆盖项（无覆盖则写"无"）

> 档案「通用约定·全局段」为默认值；本应用如需不同（主键策略/软删除/分页实现等），在此显式声明并注明理由。

| 覆盖项 | 全局默认 | 本应用 | 理由 |
|--------|----------|--------|------|
| {如：主键策略} | {雪花ID} | {AUTO_INCREMENT} | {历史库遗留} |

## 五、标杆模块

- {标杆模块}：{代码路径}（{参考点}）。

## 六、技术栈特有检查项（注入 /module-review）

- {如：接口级权限注解全覆盖}；
- {如：按钮权限指令控制显隐}。
