# 前端开发规范（类型层）

> 定位：**所有前端类应用（web 管理端 / 移动端 / 小程序 / 大屏…）共享的共性约定**。
> 分层纪律：本文件只写跨前端共性；各应用的栈细节（UI 组件库、路由机制、权限指令、目录结构）一律写在 `规范/应用-{应用标识}.md`，**本文件不复制应用层内容，应用层不复制本文件内容**（防双维护）。
> **渲染义务（初始化时）**：本文件所有 `{占位符}` 必须替换为项目实际值，替换后不得残留任何 `{...}`；可用 `doc-framework check` 扫描占位符残留验证。

## 零、引用与优先级（唯一入口，防双维护）

- **本文件**：前端类型层共性（速查索引），`/module-doc` 导航区与 `/module-review` 按此引用；
- **应用层**：`规范/应用-{应用标识}.md`（每个前端应用一份，承载栈细节与目录结构）；
- **全局层**：`{文档根}/接口规范.md`（认证/错误码/分页/接口兼容性，所有应用一致）；
- **冲突判定**：全局层 > 类型层（本文件） > 应用层（应用层只可细化，不得推翻上层）；
- **纪律**：逐层引用、禁止复制全文（避免双维护漂移）。

## 一、分层与目录（原则）

- 分层结构：视图（View）→ API 封装 → 状态管理 → 组件库；
- 目录约定：按档案「应用清单」中该应用的代码根；应用内目录结构（页面分层、组件组织）见对应 `规范/应用-{应用标识}.md`；
- 模块落点：一个业务模块在某前端应用的落点（页面/组件/api 文件位置）登记在模块契约 §5 应用落点表，不在规范重复。

## 二、命名

- 文件/组件：kebab-case（目录与文件名）；组件名 PascalCase；
- 权限标识：`{域}:{实体}:{操作}`（**跨应用不变量**，所有端完全一致；控制方式按端而异，见应用层）；
- 路由命名与菜单/页面注册保持一致（具体机制见应用层）。

## 三、权限体系（共性部分）

- 权限标识格式不变量：`{域}:{实体}:{操作}`，全端统一；
- 按钮/入口显隐：按 `status`（状态机）+ 角色计算属性动态控制（状态机语义见模块契约，**跨端一致**）；
- 具体指令/函数（如 `v-hasPermi` 或自研实现）见对应应用规范。

## 四、通用约定

- 请求封装：统一请求实例（拦截器处理 Token、错误码、401 跳转）——各端实现可不同（axios / uni.request），拦截语义一致；
- API 文件：一个模块一个文件，函数命名 `listXxx / getXxx / addXxx / updateXxx / delXxx`；
- 状态管理：跨页面共享数据入状态管理，页面内状态用组件自身状态；
- 附件上传：统一上传组件（`action={后端}/common/upload`），成功后取返回 URL 组装附件对象，不在前端拼接业务表附件字段；
- 日期时间：统一 `yyyy-MM-dd HH:mm:ss`（详情/编辑按字段粒度用 `yyyy-MM-dd`）；
- 字段展示子集/派生字段：**允许因端而异**（跨应用可变量），但枚举 `code`、状态语义禁止重新定义。

## 五、标杆模板（类型级）

- {标杆模块1}（{应用标识}）：{代码路径}（{参考点}）。
