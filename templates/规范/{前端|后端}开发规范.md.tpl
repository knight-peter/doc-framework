# {前端|后端}开发规范

> 全局编码约定（与技术栈无关的通用规则；技术栈专属细节按项目实际填写）。
> 维护：随框架升级或约定变化而更新；模块契约只写模块特有内容，通用规则引用本文件。

## 一、分层与目录

- 分层结构：{分层说明}（如：Controller → Service → Mapper → 数据库；视图 → API 封装 → 状态管理）；
- 目录约定：按项目档案"目录约定"（{代码根目录}）；
- SQL 变更脚本：`sql/change/YYYY-MM-DD-{模块}_{说明}.sql`（不修改历史建表脚本）。

## 二、命名

- 类/文件：{命名约定}（如 PascalCase / kebab-case）；
- 接口权限标识：`{域}:{实体}:{操作}`；
- 数据库表：{表命名约定}；字段：{字段命名约定}（如 snake_case）；
- 枚举类：{枚举命名约定}（状态/结果等）。

## 三、权限体系

- 后端：{权限注解约定}（接口级权限全覆盖）；
- 前端：{按钮权限指令约定}；
- 写操作校验：岗位优先 + 单位/关系兜底的双层校验（详见模块契约）。

## 四、通用约定

- 主键：{主键策略}（新增表按档案约定，不擅自改变全局策略）；
- 软删除：{软删除约定}（查询/更新 SQL 必须携带 del_flag 条件）；
- 审计字段：{审计字段}（创建/更新时自动填充）；
- 乐观锁：{乐观锁约定}（并发写操作必须校验版本号）；
- 分页：{分页约定}；
- 事务：{事务约定}（多表写操作必须事务）。

## 五、标杆模板

- {标杆模块1}：{代码路径}（{参考点}）；
- {标杆模块2}：{代码路径}（{参考点}）。
