# 前端开发规范

> 全局前端编码约定。维护：随框架升级或约定变化而更新；模块契约只写模块特有内容，通用规则引用本文件。
> **渲染义务（初始化时）**：本文件所有 `{占位符}` 必须替换为项目实际值（UI 框架、状态管理、代码根目录、标杆模块等），替换后不得残留任何 `{...}`；可用 `doc-framework check` 扫描占位符残留验证。

## 零、引用与优先级（唯一入口，防双维护）

- **本文件**：doc-framework 体系入口（速查索引），`/module-doc` 导航区与 `/module-review` 按此引用；
- **详细模板/用法**：`{文档根}/standards/前端开发规范.md`（项目原始规范，如存在）；
- **全局编码约束**：{全局约束文件}（如 `{文档根}/standards/前端开发规范.md` 附 A 或 `.claude/CLAUDE.frontend.md`，按项目实际；不存在则本文件直接承载约束要点）；
- **冲突判定**：{全局约束文件}（全局约束） > `{文档根}/standards/前端开发规范.md`（详细模板） > 本文件（速查索引）；
- **纪律**：本文件只做索引与摘要，详细内容一律引用原始规范，**禁止在本文件复制全文**（避免双维护漂移）。

## 一、分层与目录

- 技术栈（**必填渲染**）：UI 组件库 {UI 组件库}（如 Element Plus / Element UI / Ant Design Vue）、状态管理 {状态管理}（如 Pinia / Vuex）、构建工具 {构建工具}（如 Vite / Vue CLI）；
- 分层结构：视图（View）→ API 封装 → 状态管理（{状态管理}）→ 组件库（{UI 组件库}）；
- 目录约定：按项目档案"目录约定"（{前端代码根目录}），页面分层按 {页面分层约定}（如 系统/业务/流程，按项目实际）；
- 模块内部组织：
  - 标准 CRUD：`views/{层级}/{模块}/index.vue` + `components/{XxxSearch|XxxTable|XxxDetailDialog|XxxModifyDialog}.vue` + `api/{业务域}/{模块}.js`；
  - 复杂工作流：`views/{层级}/{模块}/index.vue`（单页流程）+ `components/{XxxSubmitDialog|XxxAuditDialog|XxxDetailDialog}.vue`。

## 二、命名

- 文件/组件：kebab-case（目录与文件名）；组件名 PascalCase；
- 权限标识：`{域}:{实体}:{操作}`（`v-hasPermi` 指令控制按钮显隐）；
- 路由：按档案目录约定命名，`route_name` 与菜单表 `sys_menu.route_name` 一致。

## 三、权限体系

- 前端：按钮权限用 `v-hasPermi="['{域}:{实体}:{操作}']"` 指令控制显隐；
- 写操作按钮显隐：按 `status`（状态机）+ 角色计算属性动态控制（详见模块契约）；
- 列表页：搜索/表格/分页拆分组件，所有请求携带{项目上下文参数}（多项目/多单位数据隔离场景，按项目实际；如 projectId 仅为示例）。

## 四、通用约定

- 请求封装：统一 `request` 实例（拦截器处理 Token、错误码、401 跳转）；
- API 文件：一个模块一个文件，函数命名 `listXxx / getXxx / addXxx / updateXxx / delXxx`；
- 状态管理：跨页面共享数据入状态管理，页面内状态用组件 data；
- 附件上传：统一上传组件（`action={后端}/common/upload`），成功后取返回 URL 组装附件对象，不在前端拼接业务表附件字段；
- 日期时间：统一 `yyyy-MM-dd HH:mm:ss`（详情/编辑按字段粒度用 `yyyy-MM-dd`）。

## 五、全局约束索引（详细见 {全局约束文件}）

| 约束域 | 关键点 | 详细章节 |
|--------|--------|----------|
| API 调用 | 接口文件组织、request 配置、Token 请求头、防重复提交 | {全局约束文件} §1 |
| 组件 | 组件命名、组件组织、全局组件注册 | {全局约束文件} §2 |
| 样式 | 技术选择（scss）、样式穿透、命名约定 | {全局约束文件} §3 |
| 路由 | 路由类型、Meta 约定、导航守卫 | {全局约束文件} §4 |
| 状态管理 | 状态管理模块清单、使用约定 | {全局约束文件} §5 |
| 权限控制 | 权限标识格式、指令级控制、JS 级控制 | {全局约束文件} §6 |
| 全局能力 | 全局方法、插件（Vue.prototype 挂载） | {全局约束文件} §7-8 |
| 环境变量 | 环境变量清单与使用 | {全局约束文件} §9 |

## 六、标杆模板

- {标杆模块1}：{代码路径}（{参考点，如标准 CRUD 四件套}）；
- {标杆模块2}：{代码路径}（{参考点，如复杂工作流单页流程}）。
