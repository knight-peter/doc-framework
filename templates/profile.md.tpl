# 项目档案

> 本文件是面向文档开发体系的**项目级配置**（唯一需人工确认的文件）。skill（/module-explore /module-doc /module-plan /module-code /module-review）一切配置从此读取。
> 生成方式：接入初始化时由 AI 按《接入指南.md》扫描代码库生成草案 → 开发者确认/修正。

## 应用清单（skill 的路由表，一次配置永久生效）

> 工作区内的全部应用（前端端 / 后端服务 / 聚合层 / 共享库）。**所有 skill 先读本表确定本次涉及的应用集合**，再只加载这些应用的规范与标杆（可见性 ≠ 上下文）。
> 纪律：**新增应用先登记本表再使用**；代码根按现状登记（任意相对路径，禁止与其他应用代码根互相嵌套）。
> 兼容：单前端单后端项目只登记两行，行为与旧版档案等价。

| 应用标识 | 类型 | 技术栈 | 代码根 | 规范文件 | 数据库/存储 | 依赖的应用 |
|----------|------|--------|--------|----------|-------------|------------|
| {app-web} | 前端端 | {如 Vue 3 / Element Plus / Pinia / Vite} | {代码根路径} | `规范/应用-{app-web}.md` | — | {svc-bff} |
| {app-mobile} | 前端端 | {如 uni-app / Vue 3 / uview-plus} | {代码根路径} | `规范/应用-{app-mobile}.md` | — | {svc-bff} |
| {svc-main} | 后端服务 | {如 Java 17 / Spring Boot 3 / MyBatis-Plus / MySQL 8} | {代码根路径} | `规范/应用-{svc-main}.md` | {如 mysql: order_db} | — |
| {svc-bff} | 聚合层(BFF) | {如 Node / NestJS} | {代码根路径} | `规范/应用-{svc-bff}.md` | — | {svc-main} |

- 类型枚举：`前端端` / `后端服务` / `聚合层(BFF)` / `共享库`；
- 「依赖的应用」：调用方向（如 BFF → 业务服务），与总契约 §三 应用拓扑一致；
- 「数据库/存储」：服务端应用必填（库层断言用），前端端填 `—`。

## 工作流（可选）
- 工作流引擎: {工作流引擎，可选}

## 文档语言（二选一，全项目统一，默认中文；语言由根目录名识别，内部命名全中文或全英文）
- 文档语言: 中文（根目录 `doc-framework/`：档案 doc-framework/项目档案.md，模块 doc-framework/模块/{模块名}/契约.md）
  或 英文（根目录 `doc-framework-en/`：档案 doc-framework-en/profile.md，模块 doc-framework-en/modules/{module}/contract.md）

## 文档导航（skill 自动发现用，一次配置永久生效）
- 总契约: {文档根}/总契约.md（应用拓扑、模块索引表、依赖矩阵）
- 边界文档: {文档根}/边界/{能力}边界.md（按实际增删，如工作流集成、附件管理、服务间调用、鉴权网关）
- 规范文档: {文档根}/规范/类型-前端.md、{文档根}/规范/类型-后端.md（类型层共性）；{文档根}/规范/应用-{应用标识}.md（每应用一份差异）
- 模块文件夹: {文档根}/模块/{模块名}/（契约.md / 接口.md / 测试.md / test.sh）

## 通用约定（全局段：所有应用一致；应用级差异在对应 `规范/应用-{应用标识}.md` 显式覆盖并注明理由）
- 主键: {主键策略}（全局默认，如雪花ID（服务端生成）；应用可覆盖）
- 软删除: {软删除约定}（如 del_flag 0/2）
- 审计字段: {审计字段}（如 create_by/create_time/update_by/update_time）
- 业务域: {域清单见 总契约.md §二}（域 = 权限标识前缀 + 模块分组标签，不定义架构边界；新域先登记）
- 权限标识格式: {域}:{实体}:{操作}
- 分页: {分页约定}（如页码/页大小参数 + 返回 total）
- SQL 变更: 各服务端应用 `{代码根}/sql/change/YYYY-MM-DD-*.sql`（按服务各自的目录；不修改历史建表脚本）

## 模块形态 → 标杆映射（形态清单按本项目实际探测确认，可增删；标杆按应用分别给出）
| 模块形态 | 应用 | 标杆模块 | 代码路径 |
|----------|------|----------|----------|
| {形态1} | {应用标识} | {标杆模块} | {代码路径} |
| {形态2} | {应用标识} | {标杆模块} | {代码路径} |

> 常见形态示例：简单 CRUD / 单级审批流 / 多级协同流程 / 报表查询 / 纯展示页 / 定时任务（按项目实际增删）。

## 技术栈特有检查项（注入 /module-review，按应用分列）
- {应用标识}: {如：继承统一 Controller 基类、接口级权限注解全覆盖、构造器注入}
- {应用标识}: {如：按钮权限指令控制、列表页布局组件、搜索表单组件}
