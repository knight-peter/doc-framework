# 项目档案

> 本文件是契约驱动开发体系的**项目级配置**（唯一需人工确认的文件）。skill（/文档 /代码 /检查）一切配置从此读取。
> 生成方式：接入初始化时由 AI 按《接入指南.md》扫描代码库生成草案 → 开发者确认/修正。

## 技术栈
- 后端: {后端框架与技术栈}（如 Java 17 / Spring Boot 3 / MyBatis-Plus / MySQL 8）
- 前端: {前端框架与技术栈}（如 Vue 3 / Element Plus / Pinia / Vite）
- 工作流: {工作流引擎，可选}

## 文档语言（二选一，全项目统一，默认中文）
- 文档语言: 中文（档案 doc/项目档案.md，模块 doc/模块/{模块名}/契约.md）
  或 英文（docs/profile.md，模块 docs/modules/{module}/contract.md）

## 目录约定
- 后端代码: {后端代码根目录}
- 前端代码: {前端代码根目录}
- SQL 变更: {SQL 变更脚本目录}（如 sql/change/YYYY-MM-DD-*.sql）

## 文档导航（skill 自动发现用，一次配置永久生效）
- 总契约: {文档根}/总契约.md（模块索引表、依赖矩阵）
- 边界文档: {文档根}/边界/{能力}边界.md（按实际增删，如工作流集成、附件管理）
- 规范文档: {文档根}/规范/后端开发规范.md、{文档根}/规范/前端开发规范.md
- 模块文件夹: {文档根}/模块/{模块名}/（契约.md / 接口.md / 测试.md / test.sh）

## 模块形态 → 标杆映射（形态清单按本项目实际探测确认，可增删）
| 模块形态 | 标杆模块 | 代码路径 |
|----------|----------|----------|
| {形态1} | {标杆模块} | {代码路径} |
| {形态2} | {标杆模块} | {代码路径} |

> 常见形态示例：简单 CRUD / 单级审批流 / 多级协同流程 / 报表查询 / 纯展示页 / 定时任务（按项目实际增删）。

## 通用约定
- 主键: {主键策略}（如雪花ID（服务端生成）/ AUTO_INCREMENT）
- 软删除: {软删除约定}（如 del_flag 0/2）
- 审计字段: {审计字段}（如 create_by/create_time/update_by/update_time）
- 权限标识格式: {域}:{实体}:{操作}
- 分页: {分页约定}（如页码/页大小参数 + 返回 total）

## 技术栈特有检查项（注入 /检查）
- 后端: {如：继承统一 Controller 基类、接口级权限注解全覆盖、构造器注入}
- 前端: {如：按钮权限指令控制、列表页布局组件、搜索表单组件}
