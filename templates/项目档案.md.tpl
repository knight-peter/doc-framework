# 项目档案

<!-- doc-framework:docstruct {{框架版本}} | 全局文档结构版本；初始化渲染时替换为当时的框架版本（仅一个来源：package.json），框架升级后请人工对齐，见框架仓库 docs/设计文档.md §9.3 -->

> 本文件是面向文档开发体系的**项目级配置**（唯一需人工确认的文件）。skill（/module-explore /module-doc /module-plan /module-code /module-review）一切配置从此读取。
> 生成方式：接入初始化时由 AI 按《接入指南.md》扫描代码库生成草案 → 开发者确认/修正。
> **渲染义务**：双花括号字段必须替换为项目实际值；方括号是示例/可选项，填好后删除；单花括号是路径与命名记法（如 `{模块名}`、`{域}:{实体}:{操作}`），照原样保留。

## 应用清单（skill 的路由表，一次配置永久生效）

> 工作区内的全部应用（前端端 / 后端服务 / 聚合层 / 共享库）。**所有 skill 先读本表确定本次涉及的应用集合**，再只加载这些应用的规范与标杆（可见性 ≠ 上下文）。
> 纪律：**新增应用先登记本表再使用**；代码根按现状登记（任意相对路径，禁止与其他应用代码根相互嵌套）。
> 兼容：单前端单后端项目只登记两行，行为与旧版档案等价。

| 应用标识 | 类型 | 技术栈 | 代码根 | 规范文件 | 数据库/存储 | 依赖的应用 |
|----------|------|--------|--------|----------|-------------|------------|
| {{app-1}} | {{类型-1}} | {{技术栈-1}} | {{代码根-1}} | `规范/应用-{{app-1}}.md` | {{数据库/存储-1}} | {{依赖的应用-1}} |
| {{app-2}} | {{类型-2}} | {{技术栈-2}} | {{代码根-2}} | `规范/应用-{{app-2}}.md` | {{数据库/存储-2}} | {{依赖的应用-2}} |

> 每多一个应用**增一行**（上面两行按项目实际数量增删）。填写口径：
> - 类型枚举：`前端端` / `后端服务` / `聚合层(BFF)` / `共享库`；
> - 技术栈：【如 `Vue 3 / Element Plus / Vite`；或 `Java 17 / Spring Boot 3 / MyBatis-Plus`；或 `Node / NestJS`】；
> - 代码根：【按现状登记任意相对路径，如 `apps/web`、`services/order`】；
> - 「数据库/存储」：服务端应用必填（库层断言用），【如 `mysql: order_db`】；前端端填 `—`；
> - 「依赖的应用」：调用方向【如 `svc-bff → svc-order`】，与总契约 §3 应用拓扑一致；无依赖填 `—`。

## 工作流（可选）
- 工作流引擎: {{工作流引擎}}（【如 Flowable / Activiti；无则填 `无`】）

## 文档命名（固定，不可选）
- 文档根: `doc-framework/`（档案 `doc-framework/项目档案.md`，模块 `doc-framework/模块/{模块名}/契约.md`）
- 命名: 目录 / 文件 / 章节 / 头部字段 / 枚举值**一律中文**；**英文只影响文档内容，不影响命名**（v2.6.0 起不再支持英文产物命名，见框架仓库 docs/设计文档.md §8.3）

## 文档导航（skill 自动发现用，一次配置永久生效）
- 总契约: {文档根}/总契约.md（应用拓扑、模块索引表、依赖矩阵）
- 边界文档: {文档根}/边界/{能力}边界.md（按实际增删，如工作流集成、附件管理、服务间调用、鉴权网关）
- 规范文档: {文档根}/规范/类型-前端.md、{文档根}/规范/类型-后端.md（类型层共性）；{文档根}/规范/应用-＜应用标识＞.md（**每应用一份**，逐应用替换为真实标识）
- 模块文件夹: {文档根}/模块/{模块名}/（契约.md / 接口.md / 测试.md / test.sh）

## 通用约定（全局段：所有应用一致；应用级差异在对应 `规范/应用-{应用标识}.md` 显式覆盖并注明理由）

> **本段是字段名与取值的唯一真相**（主键 / 软删除 / 审计字段 / 权限标识格式 / 分页参数与响应形状）：其他文档（类型层规范、接口规范、模块契约、模块接口.md）**只回链本段，不复制取值**——复制即双维护，是漂移高发点。
> 本段只定义"字段长什么样"；它们在接口行为上的语义（统一响应外层、分页请求参数、错误码格式）见 `接口规范.md`（跨应用不变量本身的约定见 `总契约.md` 的「维护规则」章）。

- 主键: {{主键策略}}（全局默认，【如 雪花ID（服务端生成）；应用可覆盖】）
- 软删除: {{软删除约定}}（【如 del_flag 0/2】）
- 审计字段: {{审计字段}}（【如 create_by/create_time/update_by/update_time】）
- 业务域: {{业务域清单}}（【列在 总契约.md §2】；域 = 权限标识前缀 + 模块分组标签，不定义架构边界；新域先登记）
- 权限标识格式: {域}:{实体}:{操作}（**命名记法，照原样保留**）
- 分页: {{分页约定}}（【如 页码/页大小参数 + 返回 total】；接口层形态见 接口规范.md）
- SQL 变更: 各服务端应用 `{代码根}/sql/change/YYYY-MM-DD-*.sql`（按服务各自的目录；不修改历史建表脚本）

## 模块形态 → 标杆映射（形态清单按本项目实际探测确认，可增删；标杆按应用分别给出）
| 模块形态 | 应用 | 标杆模块 | 代码路径 |
|----------|------|----------|----------|
| {{形态}} | {{app}} | {{标杆模块}} | {{代码路径}} |

> 常见形态示例：简单 CRUD / 单级审批流 / 多级协同流程 / 报表查询 / 纯展示页 / 定时任务（按项目实际增删）。

## 技术栈特有检查项（注入 /module-review，按应用分列）
- {{app}}: {{检查项-1}}【如：继承统一 Controller 基类、接口级权限注解全覆盖、构造器注入】
- {{app}}: {{检查项-2}}【如：按钮权限指令控制、列表页布局组件、搜索表单组件】
