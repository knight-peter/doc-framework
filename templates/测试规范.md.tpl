# 测试规范（通用方法论）

> 适用范围：项目内所有模块的自动化测试（工作流模块与非工作流模块）。
> 模块专属用例见 `{文档根}/模块/{模块名}/测试.md`，可执行脚本见 `{文档根}/模块/{模块名}/test.sh`。

## 一、测试策略：接口级 E2E + 库级断言（双层验证）

```
接口层（curl 真实调用后端）           库层（SQL 查业务库 + 工作流引擎库）
登录拿 JWT → 调业务接口             复核主表状态反写 + 流程运行时/历史表
→ 触发真实流程流转                   断言落库事实与引擎状态一致
```

- 接口层验证"真实行为"：提交启动流程、待办查询、审批、删除清理；
- 库层验证"落库事实"：主表 `status` / `process_instance_id` / `del_flag`，流程运行时任务与历史实例。

## 二、模块接入步骤

1. **准备账号矩阵**（从 `sys_user` / `sys_role` / `sys_user_role` / 项目人员表查询）：
   - 填报账号：具备该模块新增/编辑权限、且属于中标单位；
   - 审批账号：具备审核权限、且岗位命中流程节点角色。
2. **准备业务数据**（从{业务主表/关联表}确认）：激活{业务主数据}（按项目实际，如 项目、招标、中标单位），提交人须满足权限校验；
3. **确认流程定义**：流程 key 与 BPMN `process.id`、引擎流程定义表一致（如适用）。
4. **编写用例**（见下模板）+ **验证 SQL**（见下模板）。
5. **执行与记录**：结果回填测试文档"执行记录"节（只留最近 5 次）。

## 三、通用用例模板

### 3.1 单级审批通用用例

| 用例 | 步骤 | 断言 |
|------|------|------|
| 提交启动流程 | 填报账号登录 → 保存 → 提交 | 主表待审核状态 + `process_instance_id` 反写 |
| 待办查询 | 审批账号登录 → 工作台待办（流程 key 过滤） | 能查到待办，取 `taskId` |
| 审批通过 | 审核接口（taskId + 意见） | 主表终态；流程实例结束；待办消失 |
| 审批驳回 | 审核接口（驳回 + 意见必填） | 主表回草稿；`process_instance_id` 保留（重提复用实例，不新建） |

### 3.2 多级审批补充用例

| 用例 | 步骤 | 断言 |
|------|------|------|
| 逐节点审核 | 每节点用对应角色账号取待办 → 通过 | 主表状态按契约逐节点推进；待办逐节点转移 |
| 中间节点驳回（回发起人） | 中间节点驳回 | 主表回发起人状态；重提复用运行实例，不新建 |
| 中间节点驳回（回中间节点） | 后置节点驳回 | 主表保持待审核；流程回中间节点，重新操作后再推进 |
| 非审批节点推进 | 先执行业务动作接口，再执行推进接口 | 业务动作仅写业务字段、状态不变；推进接口才流转 |

> 状态枚举、节点顺序、驳回回退目标**一律以模块契约为准**。

## 四、通用验证 SQL 模板

```sql
-- 业务主表状态与流程实例反写
SELECT id, status, process_instance_id, del_flag
FROM {业务主表} WHERE {过滤字段} = {过滤值} ORDER BY create_time DESC LIMIT 10;

-- 流程运行中任务（如适用，{流程key} 按契约填写）
SELECT * FROM {流程引擎库}.ACT_RU_TASK WHERE PROC_DEF_ID_ LIKE '{流程key}:%';

-- 流程历史实例（如适用）
SELECT * FROM {流程引擎库}.ACT_HI_PROCINST
WHERE PROC_DEF_ID_ LIKE '{流程key}:%' ORDER BY START_TIME_ DESC LIMIT 5;
```

## 五、执行规范

- 脚本位于 `{文档根}/模块/{模块名}/test.sh`（由共享模板 `templates/scripts/e2e_template.sh` 复制改造）；
- 全部参数化：`BASE_URL` / 账号密码 / `PROJECT_ID` / `BIDDING_ID`；
- 逐步打印 PASS/FAIL 与关键 ID；
- **脚本只创建数据，不含任何删除/清理逻辑**，测试数据保留供人工复核。

## 六、缺陷处理流程（测试 → 契约闭环）

1. **定性**：自动测试断言失败或库级复核异常 → 判定是**代码 Bug**（实现偏离契约）还是**契约缺口**（约定未覆盖场景）；
2. **修复**：代码 Bug 按实施流程修复；契约缺口先补充契约约定再改代码；
3. **回写**：涉及边界/API/状态机/权限/工作流约定的变更，同步更新模块契约与边界文档；纯代码缺陷仅更新测试执行记录；
4. **复验**：修复后重跑脚本确认全绿，登记执行记录，并横向排查其他模块同类缺陷。
