# 项目规则（doc-framework 接入）

<!-- CONTRACT-FRAMEWORK-BEGIN -->
<!-- ↓↓↓ 接入引导段：初始化完成后由 AI 移除（保留永久段）↓↓↓ -->

## 项目接入（一次性）

本项目使用面向文档开发体系（doc-framework）。若 `{文档根}/项目档案.md` 不存在（项目尚未接入），AI 应主动提示用户执行接入初始化：按项目根《接入指南.md》执行（扫描代码库 → 档案草案 → 开发者确认 → 生成骨架与第一份契约）。初始化完成后：移除本引导段、删除《接入指南.md》。

<!-- ↑↑↑ 接入引导段结束 ↑↑↑ -->
<!-- CONTRACT-FRAMEWORK-END -->

## 通用约束（永久段）

- 文档语言：{文档语言}（目录与文件命名要么全中文、要么全英文，禁止混用）。
- 文档根目录：`{文档根}/`（总契约、测试规范、接口规范、模块/、边界/、规范/、计划/）。
- 模块文档四件套：`{文档根}/模块/{模块名}/契约.md`、`接口.md`、`测试.md`、`test.sh`——文档归 /文档 维护，/代码 不直接修改文档。
- 实施计划文档：`{文档根}/计划/YYYY-MM-DD-{主题}实施.md`（**文件名必须带日期前缀**，便于归档；契约文档不带日期，演进记录在"变更记录"章节）。
- skill 触发命令（仅 /命令 显式触发，不用自然语言关键词；**DSH 只识别英文技能名**，中文短命令为方法论约定）：
  - `/module-doc {模块名}` —— 模块四件套文档（创建/逆向提炼/更新/同步）
  - `/module-code {模块名}` —— 按实施计划实施代码（契约为约束），完成后回写
  - `/module-review {模块名}` —— 模块审查（含回写查账）
- 编码纪律：先文档后代码；测试发现问题先定性（代码 Bug vs 契约缺口），涉及约定变化必回写契约。
