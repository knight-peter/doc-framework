# 项目规则（doc-framework 接入）

<!-- CONTRACT-FRAMEWORK-BEGIN -->
<!-- ↓↓↓ 接入引导段：初始化完成后由 AI 移除（保留永久段）↓↓↓ -->

## 项目接入（一次性）

本项目使用面向文档开发体系（doc-framework）。若 `{文档根}/项目档案.md` 不存在（项目尚未接入），AI 应主动提示用户执行接入初始化：按项目根《接入指南.md》执行（扫描代码库 → 档案草案 → 开发者确认 → 生成骨架与第一份契约）。初始化完成后：移除本引导段、删除《接入指南.md》。

<!-- ↑↑↑ 接入引导段结束 ↑↑↑ -->
<!-- CONTRACT-FRAMEWORK-END -->

## 通用约束（永久段）

- 文档根与命名（固定）：`{文档根}/`（总契约、测试规范、接口规范、模块/、边界/、规范/、计划/、探索/）——目录 / 文件 / 章节 / 头部字段 / 枚举值**一律中文**，**不再支持英文产物命名**（英文只影响文档内容；理由与迁移见框架仓库 docs/设计文档.md §8.3）。**所有生成文档的一级章一律阿拉伯数字（`## N. 标题`，二级 `### N.M`）**——总契约 §1 文档体系 / §2 业务域 / §3 应用拓扑 / §4 模块索引表 / §5 模块依赖矩阵与全景图（§5.1 矩阵 / §5.2 全景图）/ §6 业务流程 / §7 维护规则 / §8 使用流程；模块契约 §1–§10；计划 §0–§11（「语义增量」「建模补充」两节**不编号**，是解析锚点）；类型层/应用层规范、边界文档、探索记录、模块测试文档同规则。**禁止再写 `## 一、标题` 这类中文数字章号**（v2.4.0 起统一）。
- 文档根目录：`{文档根}/`（总契约、测试规范、接口规范、模块/、边界/、规范/、计划/、探索/）。**所有生成文档的一级章一律阿拉伯数字（`## N. 标题`，二级 `### N.M`）**——总契约 §1 文档体系 / §2 业务域 / §3 应用拓扑 / §4 模块索引表 / §5 模块依赖矩阵与全景图（§5.1 矩阵 / §5.2 全景图）/ §6 业务流程 / §7 维护规则 / §8 使用流程；模块契约 §1–§10；计划 §0–§11（「语义增量」「建模补充」两节**不编号**，是解析锚点）；类型层/应用层规范、边界文档、探索记录、模块测试文档同规则。**禁止再写 `## 一、标题` 这类中文数字章号**（v2.4.0 起统一）。
- **产物命名固定中文**（v2.6.0 起）：文档根 `doc-framework/`，目录/文件/章节/头部字段/枚举值一律中文（完整清单见模块文档 / 初始化指南；**不再支持英文产物命名**——英文只影响文档内容）。老项目里的历史英文枚举（`merged`/`n/a`）与混排目录名（`modules/…/plans/`）仍被识别为**数据兼容别名**，只用于读得进老文档。
- **应用清单**：`{文档根}/项目档案.md` 的「应用清单」是全部 skill 的路由表（应用 → 类型/代码根/规范文件/数据库/依赖）；新增应用先登记清单再使用；代码路径一律从清单派生，禁止写死目录名。
- 模块核心文档：`{文档根}/模块/{模块名}/契约.md`、`接口.md`——文档归 /module-doc 维护，/module-code 不直接修改文档。测试.md 与 test.sh 为可选配套，通过 `/module-test` 按需生成。契约 §5 应用落点表登记"本业务在哪些应用存在"；落点先登记后实施（先写进计划的「语义增量」节，实施后由 `/module-doc` M 合并进本表）。
- **模块三态**（判据只有一条：**有没有实现**）：**待实现模块**（代码库中尚无任何实现——无目录/路由/表/接口/契约；有**任何一处**实现即不是，英文同义词 `greenfield`）→ `/module-explore`（探索记录**强制落盘**）→ `/module-plan` **首次建模计划**（判定式：无 `依据模块契约` ∧ 有 `依据探索记录`）→ 实施 → `/module-doc` M 的 `create` 分支建档 + 回写总契约；**存量模块（已建档）** → 计划「语义增量」→ M 的 `update` 落账；**存量模块（未建档）** → `/module-doc` 模式 B 对账补建。**存量模块未建档 ≠ 待实现模块**（有代码就不是）。
- 探索记录（按需；**待实现模块场景强制落盘**）：`{文档根}/探索/YYYY-MM-DD-{主题}.md`——写契约前的方案探索结论，单次决策记录（不作规范依据，不参与 check 硬校验）；由 /module-explore 落盘，契约导航区可回链它回答"为什么这么设计"；待实现场景额外含「待实现模块清单与关系」节，是 `/module-plan` 生成首次建模计划的唯一跨会话输入。
- 实施计划文档：单模块 `{文档根}/模块/{模块名}/计划/YYYY-MM-DD-{主题}.md`；跨模块 `{文档根}/计划/YYYY-MM-DD-{主题}.md`（**文件名必须带日期前缀**；契约文档不带日期，演进记录在"变更记录"章节）。**计划是中间产物**：语义与事实登记落进契约后由 `/module-plan {模块名} 归档` 移入 `计划/归档/`（归档时校验契约 §9 已登记 + 写入**提交 SHA**）；**归档后的计划不自动删除**，是否清理由人手动决定（长期追溯靠契约 §9 的 SHA，不依赖计划文件存活）；计划状态（待审核/已批准等）由 /module-plan 维护，/module-code 只执行「已批准」计划；计划**形态**分 `完整 / 轻量`（头部 `> 计划形态：…`；轻量=跨应用或改动面大且不触语义的小改（**单应用 + ≤5 文件 + 无决策 → 直改通道，不建计划**），只保留 逐应用表态/变更文件清单/任务清单 三节，**不含语义增量**）；**完整计划含「语义增量」节**（ADDED / MODIFIED / REMOVED 三表 + `合并状态：待合并 / 已合并 / 无需合并`；纯实现改动写"无（纯实现改动）"并标 `无需合并`；一份语义变更只由一份计划承载，拆分计划在「主计划」列回指）；计划的「逐应用表态 + 变更文件清单」即 /module-code 的**授权作用域（白名单）**——**作用域不可就地修改**（须退回修订中重新批准），**作用域内追加任务项属记账**（可直接更新）；计划的**任务清单**（`- [ ] 组.序号`）是进度事实源——/module-code 实施时逐项勾选（**只勾事实完成的项**，未勾完不得登记「已完成」），/module-review 核对完成度。已完成/已废弃的计划由 `/module-plan {模块名} 归档` 移入同级 `计划/归档/`（归档计划不再进入 glob 清单，需路径直达）；`doc-framework list --stale {{N}}`（`{{N}}` 是命令参数名，填具体天数，如 14） 列出停滞计划（批量归档入口）。
- **改动通道（授权粒度 ∝ 爆炸半径）**：不能总用同一套文档承接所有改动——判据：触契约语义/含 SQL → 完整计划（语义写进计划的「语义增量」节）；跨应用 → 轻量计划；**单应用 + 不触语义 + 无 SQL → 直改通道**（`/module-code {模块名}` 探测后给编号确认门，边界=契约 §5 应用落点，应用级；执行后 `doc-framework diff-check --module {模块名}` 对账，并在契约 §9 记一行 `来源=实现`）。**免计划必须经用户确认（禁止 AI 自我授权）**，途中发现语义变更立即停下，**升级为完整计划并把语义写进「语义增量」节**。
- skill 触发命令（仅 /命令 显式触发，不用自然语言关键词；**DSH 只识别英文技能名**）：
  - `/module-explore {主题}` —— 探索与讨论（写契约前想清楚；不写契约/计划/代码，默认不落盘、**待实现模块场景强制落盘**）
  - `/module-doc {模块名}` —— 模块核心文档（契约/接口，**零后缀**；三模式：M 合并〔create 首次建模建档 / update 存量落账〕 / 代码对账 / 产物补齐，探测后带编号确认）
  - `/module-plan {模块名}` —— 实施计划创建/修订/状态流转/**归档**（计划归 /module-plan；归档 = 校验契约 §9 已登记 + 写提交 SHA → 移入 `计划/归档/`；不自动删除）
  - `/module-code {计划路径}` —— 执行改动（三通道：完整计划 / 轻量计划 / **直改通道**免计划小改；计划的「语义增量」+ 契约不变量为约束、白名单为边界），完成后回写
  - `/module-review {模块名}` —— 模块审查（多应用对账 + 完成度对账 + 回写查账）
- 编码纪律：**语义先于代码、记录后于代码**——**语义变更写进计划的「语义增量」节**（§1–§8 的语义：数据模型/接口/状态机/权限/应用落点；批准计划 = 语义定稿），它就是代码的约束；实施完成后由 `/module-doc` **M 把增量合并进契约正文（§1–§8）与接口.md**（**记录后于代码**：契约 = 当前代码的语义映射，永不领先代码），并做**事实登记**（契约 §9 来源 `需求 / 代码 / 实现 / 合并`、含生产 SQL 时 §10、接口.md 各应用接入状态）。例外：代码是既成事实（老模块无契约 / 体系外已改）→ `/module-doc` 模式 B 从代码反向对账。测试发现问题先定性（代码 Bug vs 契约缺口），涉及约定变化必回写契约。
- 文档模板来源：**方式 A** `node_modules/doc-framework/templates/`；**方式 B（install.sh，项目里没有 node_modules）** 按安装时写入本文件的「模板来源标记」记的仓库地址 + 版本，`git clone` 到临时目录后读其中的 `templates/`（用后即弃，勿把路径当成本地存在）。`/module-doc` 生成模块文档时参考 `templates/模块/{模块名}/`，初始化骨架文档一律从模板渲染、禁止自创结构，渲染前先按《接入指南.md》「模板 → 产出映射」核对**待填占位符全清单**（模板里还有一类 `{如 …}` 示例文案，规则见该指南「渲染纪律」第 1 条）。
