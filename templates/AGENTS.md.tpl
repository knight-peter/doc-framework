# 项目规则（doc-framework 接入）

<!-- CONTRACT-FRAMEWORK-BEGIN -->
<!-- ↓↓↓ 接入引导段：初始化完成后由 AI 移除（保留永久段）↓↓↓ -->

## 项目接入（一次性）

本项目使用面向文档开发体系（doc-framework）。若 `{文档根}/项目档案.md` 不存在（项目尚未接入），AI 应主动提示用户执行接入初始化：按项目根《接入指南.md》执行（扫描代码库 → 档案草案 → 开发者确认 → 生成骨架与第一份契约）。初始化完成后：移除本引导段、删除《接入指南.md》。

<!-- ↑↑↑ 接入引导段结束 ↑↑↑ -->
<!-- CONTRACT-FRAMEWORK-END -->

## 核心约定（永久段）

### 1. 文档根与命名

- **文档根**：`{文档根}/`（总契约、测试规范、接口规范、模块/、边界/、规范/、计划/、探索/）
- **产物命名**：目录/文件/章节/头部字段/枚举值一律**中文**（v2.6.0 起固定）；英文只影响文档内容
- **章号格式**：一级 `## N. 标题`，二级 `### N.M`——**禁止中文数字**（`## 一、`）

### 2. 应用清单（路由表）

`{文档根}/项目档案.md` 的「应用清单」是全部 skill 的路由表。新增应用先登记清单再使用；代码路径一律从清单派生，禁止写死目录名。

### 3. 模块生命周期（简版 → 详见 skill）

| 阶段 | 入口 skill |
|------|-----------|
| **待实现模块**（代码零实现）→ 先探索落盘 → 首次建模计划 | `/module-explore` → `/module-plan` |
| **存量模块**（有契约）→ 语义增量 → 实施 → 合并落账 | `/module-plan` → `/module-code` → `/module-doc`(M) |
| **存量未建档**（有代码无契约）→ 代码对账补建 | `/module-doc`(B) |
| 全流程完成后审查对账 | `/module-review` |

> **判据只有一条：有没有实现**——有**任何一处**实现（目录/路由/表/接口/契约）即不是待实现模块。**存量模块未建档 ≠ 待实现模块**（有代码就不是）。
>
> **探索记录**：`{文档根}/探索/YYYY-MM-DD-{主题}.md`（待实现场景强制落盘），是 `/module-plan` 生成首次建模计划的唯一跨会话输入。
>
> **应用落点登记**：契约 §5 落点表登记"本业务在哪些应用存在"；落点先登记后实施（先写进计划「语义增量」节，实施后由 `/module-doc`(M) 合并进本表）。

### 4. 改动通道（扩权粒度 ∝ 爆炸半径）

判据在 `/module-code` skill 中有完整判定表，摘要：
- **完整计划**：触契约语义 / 含 SQL
- **轻量计划**：跨应用 / 改动面大但不触语义
- **直改通道**：单应用 + ≤5 文件 + 无 SQL + 无决策（**须经用户确认，AI 不得自授**）；执行后 `doc-framework diff-check --module {模块名}` 对账，并在契约 §9 记一行 `来源=实现`
- 发现语义变更立即停 → 升级完整计划

### 5. 编码纪律

- **语义先于代码**：语义变更写进计划的「语义增量」节，批准计划=语义定稿；§1–§8 的语义范围：数据模型/接口/状态机/权限/应用落点
- **记录后于代码**：实施完成后由 `/module-doc`(M) 把增量合并进契约正文（§1–§8）与接口.md，并做**事实登记**（契约 §9 来源：`需求/代码/实现/合并`；含生产 SQL 时 §10；接口.md 各应用接入状态）
- **契约 = 已生效真相（应然 ≡ 实然），永不领先代码**
- **DSH 只识别英文技能名，仅 /命令 显式触发**，不用自然语言关键词：`/module-explore` `/module-doc` `/module-plan` `/module-code` `/module-test` `/module-review`
- 测试发现问题先定性（代码 Bug vs 契约缺口），涉及约定变化必回写契约
- 代码是既成事实（老模块无契约/体系外已改）→ `/module-doc`(B) 从代码反向对账

### 6. 模板来源与渲染纪律

- **方式 A**：`node_modules/doc-framework/templates/`
- **方式 B**：按 AGENTS.md 底部「模板来源标记」记录的仓库地址+版本，`git clone` 到临时目录读取（用后即弃，勿把路径当成本地存在）
- **渲染纪律**：初始化骨架文档和模块文档一律从模板渲染、**禁止自创结构**；渲染前按《接入指南.md》「模板 → 产出映射」核对待填占位符全清单（模板里还有 `{如 …}` 示例文案，只作示例，渲染时按实情替换或删除）

### 7. 文档归口原则

| 操作 | 归属 |
|------|------|
| 核心文档（契约/接口） | `/module-doc`（文档写入方）；`/module-code` **不直接修改文档** |
| 实施计划 | `/module-plan` |
| 代码改动 | `/module-code` |
| 探索讨论 | `/module-explore` |
| 测试文档/脚本（可选） | `/module-test` |
| 审查对账 | `/module-review` |