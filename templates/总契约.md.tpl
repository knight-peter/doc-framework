# 总契约

> 本文件是项目的**总索引与长期事实源**：文档体系、业务域、模块索引、维护规则。
> 维护：`/文档` 每生成/更新模块时**自动回写模块索引表**（模块名、模块文件夹、核心代码路径、标杆参考、边界依赖）。

## 一、文档体系

本项目采用面向文档开发体系（doc-framework），文档分层：

- **项目档案**：`{文档根}/项目档案.md`（技术栈、目录约定、模块形态、标杆映射、通用约定——skill 一切配置来源）
- **长期契约**：本文件（总索引）、`{文档根}/模块/{模块名}/契约.md`（模块语义约束）、`{文档根}/边界/{能力}边界.md`（跨模块能力）
- **接口文档**：`{文档根}/模块/{模块名}/接口.md`（字段级调用细节，唯一事实源）
- **测试文档**：`{文档根}/测试规范.md`（通用方法论）、`{文档根}/模块/{模块名}/测试.md`（模块用例）
- **实施计划**：`{文档根}/计划/YYYY-MM-DD-{主题}实施.md`（单次决策记录，归档后不作为规范依据）

## 二、业务域清单

> 由接入初始化时按项目实际划分，AI 与开发者确认后填写。

| 域编号 | 业务域 | 核心职责 | 代表模块 |
|:------:|--------|----------|----------|
| D0 | {业务域名} | {核心职责} | {代表模块} |
| ... | ... | ... | ... |

## 三、模块索引表（/文档 自动回写）

| 模块名 | 模块文件夹 | 核心代码路径 | 模块形态 | 标杆参考 | 边界依赖 |
|--------|-----------|--------------|----------|----------|----------|
| {模块名} | `{文档根}/模块/{模块名}/` | {代码路径} | {形态} | {标杆} | {边界文档} |

## 四、模块依赖矩阵

| 模块 | 依赖的下游模块/数据 | 被谁依赖 | 契约文件 |
|------|---------------------|----------|----------|
| {模块名} | {依赖} | {被谁依赖} | {契约路径} |

## 五、维护规则

1. **契约从代码中来**：契约文档必须基于真实代码与数据库提炼，不凭空设计；
2. **重构必更契约**：修改了模块边界、API、状态机、权限时，同步更新对应契约与接口.md；
3. **实施计划引用契约**：每次实施计划文档必须显式引用所依据的模块契约；
4. **测试问题必回写**：测试发现的行为偏差，凡涉及约定变化（边界/API/状态机/工作流）必须回写契约并登记执行记录；
5. **契约变更是唯一真相**：实施计划归档后不作规范依据，长期看契约的"变更记录"章节；
6. **一契约多计划**：一个模块契约可对应多个实施计划（按日期+主题区分），每次实施完成后回写契约变更记录。

## 六、使用流程

1. `/文档 {模块名}` —— 创建/更新模块文档（文档先行 / 逆向提炼 / 改造更新）；
2. `/代码 {模块名}` —— 按实施计划实施代码（契约为约束），完成后回写；
3. `/检查 {模块名}` —— 审查（含回写查账），CRITICAL/HIGH 清零后提交。
