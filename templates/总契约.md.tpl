# 总契约

> 本文件是项目的**总索引与长期事实源**：文档体系、业务域、应用拓扑、模块索引、依赖矩阵、业务流程、维护规则。
> 维护：`/module-doc` 每生成/更新模块时**自动回写模块索引表**（模块名、业务域、主责服务、各应用落点、标杆参考、边界依赖）与**依赖矩阵/全景图**；应用清单或调用关系变更时同步更新 §三；跨模块业务流程变更时同步更新 §六。

## 一、文档体系

本项目采用面向文档开发体系（doc-framework），文档分层：

- **项目档案**：`{文档根}/项目档案.md`（**应用清单 = skill 的路由表**、通用约定、标杆映射——skill 一切配置来源）
- **长期契约**：本文件（总索引）、`{文档根}/模块/{模块名}/契约.md`（模块语义约束 + 应用落点）、`{文档根}/边界/{能力}边界.md`（跨模块/跨服务能力）
- **接口文档**：`{文档根}/接口规范.md`（全局层约定）、`{文档根}/模块/{模块名}/接口.md`（字段级服务端真相，唯一事实源）
- **开发规范**（三层）：`{文档根}/规范/类型-前端.md`、`{文档根}/规范/类型-后端.md`（类型层共性）；`{文档根}/规范/应用-{应用标识}.md`（应用层差异，每应用一份）
- **测试文档**：`{文档根}/测试规范.md`（四层方法论）、`{文档根}/模块/{模块名}/测试.md`（模块用例）
- **探索记录**（按需）：`{文档根}/探索/YYYY-MM-DD-{主题}.md`（写契约前的方案探索结论；单次决策记录，不作规范依据，不参与 check 硬校验；由 /module-explore 落盘）
- **实施计划**：单模块 `{文档根}/模块/{模块名}/计划/YYYY-MM-DD-{主题}实施.md`、跨模块 `{文档根}/计划/YYYY-MM-DD-{主题}实施.md`（单次决策记录，归档后不作为规范依据；状态由 /module-plan 维护，含**勾选式任务清单**作为进度事实源；已完成/已废弃后由 /module-plan 归档至同级 `计划/归档/`）

## 二、业务域

> `域` 只做两件事：**权限标识前缀**（`{域}:{实体}:{操作}`）与**模块分组标签**（辅助定位相关模块）。它不是架构分层——跨域依赖是常态，不视为违规。
> 登记方式：初始化时按项目实际划分，AI 与开发者确认后填写；新域**先登记本节**才能用于权限标识。模块归属见 §四 模块索引表"业务域"列（唯一事实源）。

| 域 | 说明 |
|----|------|
| {业务域} | {一句话说明} |

## 三、应用拓扑

> 索引档案「应用清单」（唯一事实源在档案，本节只做可视化与调用关系标注）；应用增删时先改档案，再同步本节。

| 应用 | 类型 | 代码根 | 依赖的应用 |
|------|------|--------|------------|
| {app-web} | 前端端 | {代码根} | {svc-bff} |
| {svc-bff} | 聚合层(BFF) | {代码根} | {svc-main} |
| {svc-main} | 后端服务 | {代码根} | — |

```mermaid
flowchart LR
    subgraph CLIENTS["前端端"]
        W["{app-web}"]
        M["{app-mobile}"]
    end
    subgraph SERVICES["服务端"]
        BFF["{svc-bff}（聚合层）"]
        SVC["{svc-main}（业务真相）"]
    end
    W -->|"HTTP"| BFF
    M -->|"HTTP"| BFF
    BFF -->|"HTTP/MQ"| SVC
```

## 四、模块索引表（/module-doc 自动回写）

| 模块名 | 业务域 | 模块文件夹 | 主责服务 | 各应用落点 | 模块形态 | 标杆参考 | 边界依赖 |
|--------|--------|-----------|----------|------------|----------|----------|----------|
| {模块名} | {业务域} | `{文档根}/模块/{模块名}/` | {应用标识} | {svc-main: 路径; app-web: 路径; app-mobile: 路径} | {形态} | {标杆} | {边界文档} |

## 五、模块依赖矩阵与全景图

> 矩阵是**唯一事实源**（AI 维护表格），全景图由矩阵渲染——**矩阵变更必须同步重绘 §5.2 的 mermaid 图**，禁止只改一处。

### 5.1 模块依赖矩阵

| 模块 | 业务域 | 主责服务 | 依赖的下游模块/数据 | 被谁依赖 | 契约文件 |
|------|--------|----------|---------------------|----------|----------|
| {模块名} | {业务域} | {应用标识} | {依赖} | {被谁依赖} | {契约路径} |

### 5.2 模块依赖全景图（由 5.1 矩阵渲染，按业务域分组）

```mermaid
flowchart LR
    subgraph D1["{业务域}"]
        A["{模块A}"]
    end
    subgraph D2["{业务域}"]
        B["{模块B}"]
    end
    A -->|"{依赖说明}"| B
```

## 六、业务流程清单

> 只登记**跨模块**业务流程（流经 ≥2 个模块）；单模块内部流程画在模块契约 §2.3，并回链本节编号。
> 纪律：**同一流程只画一处**（BF-xx 全局唯一编号）；流程步骤需标注**执行应用**（哪个服务/端）与**状态归属服务**；新增/变更流程时做**三向对账**——流程步骤 ↔ 模块接口清单 ↔ 状态迁移，三者必须互相印证。

| 编号 | 流程 | 涉及模块 | 涉及应用 | 触发/入口 |
|------|------|----------|----------|-----------|
| BF-01 | {流程名} | {模块A、模块B} | {svc-main、app-mobile} | {入口接口/事件} |

### BF-01 {流程名}

```mermaid
flowchart TB
    {角色/模块（应用）} -->|{动作}| {模块（应用）}
```

## 七、维护规则

1. **契约从代码中来**：契约文档必须基于真实代码与数据库提炼，不凭空设计；
2. **重构必更契约**：修改了模块边界、API、状态机、权限时，同步更新对应契约与接口.md；
3. **实施计划引用契约**：每次实施计划文档必须显式引用所依据的模块契约；
4. **测试问题必回写**：测试发现的行为偏差，凡涉及约定变化（边界/API/状态机/工作流）必须回写契约并登记执行记录；
5. **契约变更是唯一真相**：实施计划归档后不作规范依据，长期看契约的"变更记录"章节；
6. **一契约多计划**：一个模块契约可对应多个实施计划（按日期+主题区分），计划状态（待审核/已批准/…）由 /module-plan 维护，每次实施完成后回写契约变更记录；
7. **矩阵与全景图同步**：依赖矩阵（§5.1）是唯一事实源，依赖变更必须同时更新矩阵与全景图（§5.2）；
8. **业务流程一处一画**：跨模块流程只登记在 §六 并统一编号（BF-xx）；模块契约只画模块内部子流程并回链编号，禁止重复绘制同一流程；
9. **域先登记后使用**：新业务域先登记 §二 才能用于权限标识；域只做权限前缀与分组标签，不定义架构边界，跨域依赖不视为违规；
10. **流程三向对账**：业务流程每个步骤必须能在涉及模块的接口清单找到对应接口，每个状态迁移必须能在流程中找到触发动作；
11. **生产 SQL 必登记**：任何含需人工在生产环境执行的 SQL（建表/加字段/初始化数据/菜单权限等）的变更，必须在模块契约 §10 登记（所属服务 + 文件引用 + 影响摘要 + 执行状态），执行后回填；只写在实施计划里不算（计划归档后即失效）；
12. **跨应用不变量**：数据模型、状态机、权限标识 `{域}:{实体}:{操作}`、接口路径/响应结构/错误码在所有应用中完全一致，禁止任何端/服务自定义；字段展示子集与派生字段允许因端而异；
13. **逐应用表态**：实施计划必须对每个受影响应用显式标注「改动 / 本次不改 / 不适用」，漏表态 = 漏改；表态「本次不改」的应用代码零变更；
14. **接口语义先定于计划**：跨服务/跨端实现前，主责服务的接口**目标语义先在计划的「语义增量」节登记**（接口.md 是已生效的服务端真相，实施后由 `/module-doc` M 合并回写）；消费方按增量实现，禁止先写消费方再倒推；破坏性接口变更必须在计划中显式声明且所有消费方同步改动；
15. **落点先登记后实施**：业务扩展到新应用，先把落点写进计划的「语义增量」节（ADDED 行，目标位置 = 契约 §5），实施后由 `/module-doc` M 合并进契约 §5 应用落点表；代码有落点但契约未登记视为私建（review 判 HIGH）。

## 八、使用流程

0. `/module-explore {主题}` —— （可选，需求模糊/方案取舍时）探索与讨论，不写契约/计划/代码；**待实现模块场景强制落盘**探索记录；
1. `/module-plan {模块名}` —— 创建/修订实施计划（状态：待审核 → 已批准；含**语义增量**〔ADDED/MODIFIED/REMOVED + `合并状态`〕+ 逐应用表态 + 任务清单；**待实现模块 = 首次建模计划**：无 `依据模块契约` ∧ 有 `依据探索记录`）；
2. `/module-code {计划路径}` —— 只执行已批准的实施计划（计划的「语义增量」+ 契约不变量为约束、白名单为边界、任务逐项勾选），完成后回写；
3. `/module-doc {模块名}` —— 模块文档（零后缀；三模式自动探测：**M 合并**〔create 待实现模块首次建档 / update 存量落账〕 / B 代码对账 / C 产物补齐；确认门带编号）——把计划的增量合并进契约与接口.md（实施后**必做**）；
4. `/module-review {模块名}` —— 审查（多应用对账 + 完成度对账 + 回写查账 + 增量合并完整性 / 无计划外漂移），CRITICAL/HIGH 清零后提交；
5. `/module-plan {模块名} 归档` —— 已完成/已废弃计划移入 `计划/归档/`。
