# 总契约

> 本文件是项目的**总索引与长期事实源**：文档体系、业务域、模块索引、依赖矩阵、业务流程、维护规则。
> 维护：`/module-doc` 每生成/更新模块时**自动回写模块索引表**（模块名、业务域、模块文件夹、核心代码路径、标杆参考、边界依赖）与**依赖矩阵/全景图**；跨模块业务流程变更时同步更新 §五。

## 一、文档体系

本项目采用面向文档开发体系（doc-framework），文档分层：

- **项目档案**：`{文档根}/项目档案.md`（技术栈、目录约定、模块形态、标杆映射、通用约定——skill 一切配置来源）
- **长期契约**：本文件（总索引）、`{文档根}/模块/{模块名}/契约.md`（模块语义约束）、`{文档根}/边界/{能力}边界.md`（跨模块能力）
- **接口文档**：`{文档根}/模块/{模块名}/接口.md`（字段级调用细节，唯一事实源）
- **测试文档**：`{文档根}/测试规范.md`（通用方法论）、`{文档根}/模块/{模块名}/测试.md`（模块用例）
- **实施计划**：单模块 `{文档根}/模块/{模块名}/计划/YYYY-MM-DD-{主题}实施.md`、跨模块 `{文档根}/计划/YYYY-MM-DD-{主题}实施.md`（单次决策记录，归档后不作为规范依据；状态由 /module-plan 维护）

## 二、业务域

> `域` 只做两件事：**权限标识前缀**（`{域}:{实体}:{操作}`）与**模块分组标签**（辅助定位相关模块）。它不是架构分层——跨域依赖是常态，不视为违规。
> 登记方式：初始化时按项目实际划分，AI 与开发者确认后填写；新域**先登记本节**才能用于权限标识。模块归属见 §三 模块索引表"业务域"列（唯一事实源）。

| 域 | 说明 |
|----|------|
| {业务域} | {一句话说明} |

## 三、模块索引表（/module-doc 自动回写）

| 模块名 | 业务域 | 模块文件夹 | 核心代码路径 | 模块形态 | 标杆参考 | 边界依赖 |
|--------|--------|-----------|--------------|----------|----------|----------|
| {模块名} | {业务域} | `{文档根}/模块/{模块名}/` | {代码路径} | {形态} | {标杆} | {边界文档} |

## 四、模块依赖矩阵与全景图

> 矩阵是**唯一事实源**（AI 维护表格），全景图由矩阵渲染——**矩阵变更必须同步重绘 §4.2 的 mermaid 图**，禁止只改一处。

### 4.1 模块依赖矩阵

| 模块 | 业务域 | 依赖的下游模块/数据 | 被谁依赖 | 契约文件 |
|------|--------|---------------------|----------|----------|
| {模块名} | {业务域} | {依赖} | {被谁依赖} | {契约路径} |

### 4.2 模块依赖全景图（由 4.1 矩阵渲染，按业务域分组）

```mermaid
flowchart LR
    subgraph D1["{业务域}"]
        A["{模块A}"]
    end
    subgraph D2["{业务域}"]
        B["{模块B}"]
    end
    A -->|"{依赖说明}"| B
```

## 五、业务流程清单

> 只登记**跨模块**业务流程（流经 ≥2 个模块）；单模块内部流程画在模块契约 §2.3，并回链本节编号。
> 纪律：**同一流程只画一处**（BF-xx 全局唯一编号）；新增/变更流程时做**三向对账**——流程步骤 ↔ 模块接口清单 ↔ 状态迁移，三者必须互相印证。

| 编号 | 流程 | 涉及模块 | 触发/入口 |
|------|------|----------|-----------|
| BF-01 | {流程名} | {模块A、模块B} | {入口接口/事件} |

### BF-01 {流程名}

```mermaid
flowchart TB
    {角色/模块} -->|{动作}| {模块}
```

## 六、维护规则

1. **契约从代码中来**：契约文档必须基于真实代码与数据库提炼，不凭空设计；
2. **重构必更契约**：修改了模块边界、API、状态机、权限时，同步更新对应契约与接口.md；
3. **实施计划引用契约**：每次实施计划文档必须显式引用所依据的模块契约；
4. **测试问题必回写**：测试发现的行为偏差，凡涉及约定变化（边界/API/状态机/工作流）必须回写契约并登记执行记录；
5. **契约变更是唯一真相**：实施计划归档后不作规范依据，长期看契约的"变更记录"章节；
6. **一契约多计划**：一个模块契约可对应多个实施计划（按日期+主题区分），计划状态（待审核/已批准/…）由 /module-plan 维护，每次实施完成后回写契约变更记录；
7. **矩阵与全景图同步**：依赖矩阵（§4.1）是唯一事实源，依赖变更必须同时更新矩阵与全景图（§4.2）；
8. **业务流程一处一画**：跨模块流程只登记在 §五 并统一编号（BF-xx）；模块契约只画模块内部子流程并回链编号，禁止重复绘制同一流程；
9. **域先登记后使用**：新业务域先登记 §二 才能用于权限标识；域只做权限前缀与分组标签，不定义架构边界，跨域依赖不视为违规；
10. **流程三向对账**：业务流程每个步骤必须能在涉及模块的接口清单找到对应接口，每个状态迁移必须能在流程中找到触发动作。

## 七、使用流程

1. `/module-doc {模块名}` —— 创建/更新模块文档（文档先行 / 逆向提炼 / 改造更新）；
2. `/module-plan {模块名}` —— 创建/修订实施计划（状态：待审核 → 已批准）；
3. `/module-code {计划路径}` —— 只执行已批准的实施计划（契约为约束），完成后回写；
4. `/module-review {模块名}` —— 审查（含回写查账），CRITICAL/HIGH 清零后提交。
