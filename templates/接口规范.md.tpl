# 接口规范（全局接口约定）

<!-- doc-framework:docstruct {{框架版本}} | 全局文档结构版本；初始化渲染时替换为当时的框架版本（仅一个来源：package.json），框架升级后请人工对齐，见框架仓库 docs/设计文档.md §9.3 -->

> 适用范围：项目内所有模块的接口定义与调用。本文件约定**跨模块统一规则**：REST 风格 / 认证鉴权 / 统一响应外层与错误码 / 分页 / 接口层字段语义。
> **接口兼容性红线**（破坏性变更的处置）不在此定义——见 `总契约.md` 的「维护规则」章与计划模板的「接口兼容性声明」节。
> **本节边界**：字段名与字段级细节一律不进本文件——各模块 `接口.md` 是字段级**唯一事实源**；主键 / 软删除 / 审计字段等**字段名与取值**的唯一真相在 `项目档案.md` 的「通用约定」（本文件只约定它们在**接口层**的呈现语义，不重复定义取值）。
> 维护：本文件只承载全局规则，**不承载维护流程**——维护规则见 `总契约.md` 的「维护规则」章。

## 1. REST 风格

- 资源命名：复数名词小写，路径层级用 `/`（如 `/orders/{id}/items`；`{id}`/`{资源}`/`{动作}`/`{ids}` 是**路径记法**，照原样保留）；
- 方法语义：`GET` 查询 / `POST` 新增与动作 / `PUT` 整体更新 / `DELETE` 删除；
- 动作型接口（提交/审核/组织）：`POST /{资源}/{动作}`（如 `/project/completion/submit`）；
- 批量删除：`DELETE /{资源}/{ids}`（逗号分隔）。

## 2. 认证鉴权

- 认证方式：{{认证方式}}（如 JWT：请求头 `Authorization: Bearer <token>`）；
- 权限标识：按档案通用约定 `{域}:{实体}:{操作}`（如 `{域}:{实体}:submit`）；
- 后端接口级权限注解全覆盖；前端按钮用权限指令控制显隐；
- 写操作按{{权限校验模型}}双层校验（【如 岗位优先 + 单位/关系兜底，按项目实际】；**逐接口的校验规则**在模块契约 §4.3）。

## 3. 通用响应结构

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": { }
}
```

- 本结构是**所有接口的统一外层**，各模块 `接口.md` 只定义 `data` 内部字段，不再重复外层；
- `code`：`200` 成功；`401` 未认证；`403` 无权限；`404` 不存在；`500` 服务异常；
- 业务错误：`code` 非 200，`msg` 为可直接展示给用户的文案（不暴露堆栈）；
- 错误码格式：统一三位数字（如 `400` 参数错误 / `401` 未认证 / `403` 无权限 / `404` 不存在 / `409` 冲突 / `500` 内部错误）——**这是响应体内的 `code`**，不是 HTTP 状态码，也不带业务域前缀。

## 4. 分页约定

- 请求：`pageNum`（页码，从 1 起）/ `pageSize`（每页条数）；排序 `orderByColumn` / `isAsc`（可选）；
- 响应：`data = {"rows": [...], "total": N}`（`rows` 当前页数据 + `total` 总数）；
- 分页接口同样遵守第 3 节的统一外层，本节只约定外层内 `data` 的形状与请求参数。

## 5. 接口层字段语义（取值见档案）

> **取值与默认值的唯一真相在 `项目档案.md` 的「通用约定」**，本节只声明它们在接口行为上的语义，禁止在此复制具体取值（防双维护）。

- 审计字段：请求/响应按档案约定自动填充（创建与更新时），一般不由前端传入；
- 软删除：删除接口做逻辑删除（按档案约定），查询结果默认剔除已删除数据；
- 乐观锁：更新接口按档案约定携带版本号，版本冲突返回业务错误（`code` 非 200）而非 500。
