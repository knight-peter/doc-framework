# 接口规范（全局接口约定）

> 适用范围：项目内所有模块的接口定义与调用。**字段级细节**在各模块 `接口.md`（唯一事实源）；本文件约定**跨模块统一规则**。
> 维护：/文档 与 /代码 按"契约 §4 与接口.md 分工"协作维护（见模块契约模板）。

## 一、REST 风格

- 资源命名：复数名词小写，路径层级用 `/`（如 `/orders/{id}/items`）；
- 方法语义：`GET` 查询 / `POST` 新增与动作 / `PUT` 整体更新 / `DELETE` 删除；
- 动作型接口（提交/审核/组织）：`POST /{资源}/{动作}`（如 `/project/completion/submit`）；
- 批量删除：`DELETE /{资源}/{ids}`（逗号分隔）。

## 二、认证鉴权

- 认证方式：{认证方式}（如 JWT：`Authorization: Bearer {token}`）；
- 权限标识：按档案通用约定 `{域}:{实体}:{操作}`（如 `project:completion:submit`）；
- 后端接口级权限注解全覆盖；前端按钮用权限指令控制显隐；
- 写操作按"岗位优先 + 单位/关系兜底"双层校验（模块契约 §4 详述）。

## 三、通用响应结构

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": { }
}
```

- `code`：`200` 成功；`401` 未认证；`403` 无权限；`404` 不存在；`500` 服务异常；
- 业务错误：`code` 非 200，`msg` 为可直接展示给用户的文案；
- 分页列表：`data = { "rows": [...], "total": N }`，请求参数 `pageNum` / `pageSize`。

## 四、错误码格式

- 统一 `{域}{编号}` 三位数字（如 `400` 参数错误 / `401` 未认证 / `403` 无权限 / `404` 不存在 / `409` 冲突 / `500` 内部错误）；
- 业务校验失败优先返回具体文案（如"该工程完工申请尚未审核通过，不能上传竣工验收报告"），不暴露堆栈。

## 五、分页约定

- 请求：`pageNum`（页码，从 1 起）/ `pageSize`（每页条数）；
- 响应：`rows`（当前页数据）+ `total`（总数）；
- 排序：`orderByColumn` / `isAsc`（可选）。

## 六、通用字段

- 审计字段：`createBy/createTime/updateBy/updateTime`（按档案约定）；
- 软删除：`delFlag`（按档案约定，如 `0` 存在 / `2` 删除）；
- 乐观锁：`version`（更新 SQL `version = version + 1`，WHERE 携带 `version = #{version}`，0 行影响抛并发冲突）。
