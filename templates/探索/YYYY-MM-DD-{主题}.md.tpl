# YYYY-MM-DD {探索主题}

> 状态：{探索中 | 已定案 | 已废弃}
> 涉及应用：{应用标识 …}（由档案「应用清单」路由得出；不确定的标注"待确认"）
> 交接：{/module-plan {模块名}（存量增量 / 首次建模）| 无（仅讨论）}
> 定位：**单次决策记录**（同实施计划）——归档后不作规范依据，长期真相以事后生成的契约为准；不参与 `doc-framework check` 硬校验。
> 纪律：本记录只记"选项与理由"，**不复制契约内容**（业务语义真相永远在契约，防双维护）。

---

## 一、背景与问题

{要解决什么问题 / 触发原因 / 现状调研结论（附代码与文档证据：文件路径、既有实现、可复用能力）}

## 二、候选方案对比

| 方案 | 做法 | 涉及应用 | 改动面 | 风险 | 与现有契约/边界的冲突点 |
|------|------|----------|--------|------|--------------------------|
| A（推荐） | {做法} | {应用标识} | {文件/接口/表} | {风险} | {冲突点或"无"} |
| B | {做法} | {应用标识} | {改动面} | {风险} | {冲突点} |

> 涉及结构/流程的方案建议配 mermaid 图（人机共读）。

## 三、选定方案与理由

- **选定**：{方案 X}；
- **理由**：{为什么它最契合目标与现状（有证据）}；
- **放弃其他方案的原因**：{一句话一个}；
- **对契约的影响**：{需要新增/修改哪些契约章节（由计划的「语义增量」节承载，实施后由 M 合并进契约）；是否涉及落点变更}。

## 四、未决问题

1. {未决项}（需要谁/什么时候定）；
2. {未决项}。

> 未决问题不阻塞立项时，在契约或实施计划中显式登记为"待确认事项"，不要暗含假设。

## 五、待实现模块清单与关系

> 仅**待实现模块**场景必填（待实现模块 = 代码库中尚无任何实现的模块：无目录 / 无路由 / 无表 / 无接口 / 无契约；有任何一处实现即不是）；结论是"扩展既有模块"或"不做"时，本节写"无（不涉及待实现模块）"。
> 本节是 `/module-plan` 生成首次建模计划的输入之一（跨会话载体），也是跨模块计划「依据模块契约」指向首次建模计划的依据。

| 拟新增模块 | 职责边界（做什么 / 不做什么） | 与既有模块的调用 / 被调用关系 | 是否共享状态机或权限域 |
|------------|------------------------------|-------------------------------|------------------------|
| {模块名} | {职责 / 边界} | {调用 {模块}；被 {模块} 调用} | {共享 {状态机 / 权限域} 或 无} |
