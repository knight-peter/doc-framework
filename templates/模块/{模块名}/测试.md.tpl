# {模块名}自动测试

> 模块：{模块名}（{模块形态}）
> 依据契约：`{文档根}/模块/{模块名}/契约.md`（§3.2 状态机 / §4 接口概览 / §8 验收标准）
> 通用流程：见 `{文档根}/测试规范.md`（E2E 双层验证、用例模板、缺陷闭环）
> 执行脚本：`{文档根}/模块/{模块名}/test.sh`
> 导航区：回链 契约.md / 接口.md；关联文档 测试规范.md

---

## 一、测试目标

{验证要点列表，如：提交启动流程、逐节点审核、驳回语义、删除清理}

## 二、前置条件

| 项 | 要求 |
|----|------|
| 后端服务 | {地址可达} |
| 流程定义 | {流程 key 已部署（如适用）} |
| 表结构 | {字段已存在（变更脚本）} |
| 权限 | {账号权限要求} |

## 三、测试账号矩阵

| 角色 | 登录账号 | 姓名 | 角色/岗位 | 说明 |
|------|----------|------|-----------|------|
| {填报} | {account} | {name} | {role} | {说明} |
| {审批} | {account} | {name} | {role} | {说明} |

> 登录密码以实际环境为准，勿重置他人密码。

## 四、端到端测试用例

> 状态枚举以契约 §3.2 为准。

### 用例 1：{用例名}

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1.1 | {操作} | {预期} |
| 1.2 | {操作} | {预期} |

### 用例 2：{用例名}

| 步骤 | 操作 | 预期 |
|------|------|------|
| 2.1 | {操作} | {预期} |

> 删除功能不纳入自动测试（脚本只创建不删除），由人工界面验证。

## 五、验证 SQL

```sql
-- 主表状态与流程实例反写
SELECT id, status, process_instance_id, del_flag
FROM {业务主表} WHERE project_id = {projectId} ORDER BY create_time DESC LIMIT 10;

-- 流程运行时/历史（如适用）
SELECT * FROM {流程引擎库}.ACT_RU_TASK WHERE PROC_DEF_ID_ LIKE '{流程key}:%';
```

## 六、执行记录（只留最近 5 次）

| 日期 | 执行方式 | 结果（PASS/FAIL/跳过） | 遗留问题 |
|------|----------|------------------------|----------|
| {YYYY-MM-DD} | 脚本/手动 | {PASS} | {无} |
