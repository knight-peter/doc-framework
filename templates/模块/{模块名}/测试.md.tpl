# {模块名}自动测试

> 模块：{模块名}（{模块形态}）
> 依据契约：`{文档根}/模块/{模块名}/契约.md`（§3.2 状态机 / §4 接口概览 / §5 应用落点 / §8 验收标准）
> 通用流程：见 `{文档根}/测试规范.md`（四层验证、**§1.1 黑盒/白盒适用规则**、用例模板、缺陷闭环）
> 执行脚本：`{文档根}/模块/{模块名}/test.sh`（接口层 + 编排层；端层为本文 checklist）
> 导航区：回链 契约.md / 接口.md；关联文档 测试规范.md

---

## 一、测试目标

{验证要点列表，如：提交启动流程、逐节点审核、驳回语义、删除清理}

## 二、适用层与依据（对照 测试规范 §1.1）

> **登记义务**：逐层标注 适用/不适用，**不适用必须写原因**；`/module-review` 按此做覆盖完整性检查（黑盒缺失 = HIGH；白盒该做未做且无理由 = HIGH）。

| 层 | 类型 | 是否适用 | 依据 / 不适用原因 |
|----|------|----------|-------------------|
| 接口层 | 黑盒 | **适用（必备）** | 所有模块必备，无例外 |
| 库层 | 白盒 | {适用 / 不适用} | {适用：有主表 status 反写 / 流程实例反写 / 软删除 / 乐观锁；不适用：写原因} |
| 端层 | 黑盒 | {适用 / 不适用} | {适用：契约 §5 有前端端落点；不适用：无前端落点} |
| 编排层 | 黑盒 | {适用 / 不适用} | {适用：契约 §5 涉及 ≥2 个服务端应用；不适用：单服务模块} |

> 白盒判定依据是**模块事实**（有无状态/流程反写/软删除/乐观锁），不是"写起来麻烦"；命中即必须做库层断言。

## 三、前置条件

| 项 | 要求 |
|----|------|
| 服务端应用 | {各服务地址可达，按契约 §5 落点表逐个列出} |
| 流程定义 | {流程 key 已部署（如适用）} |
| 表结构 | {字段已存在（变更脚本）} |
| 权限 | {账号权限要求} |

## 四、测试账号矩阵

| 角色 | 登录账号 | 姓名 | 角色/岗位 | 说明 |
|------|----------|------|-----------|------|
| {填报} | {account} | {name} | {role} | {说明} |
| {审批} | {account} | {name} | {role} | {说明} |

> 登录密码以实际环境为准，勿重置他人密码。

## 五、端到端测试用例

> 状态枚举以契约 §3.2 为准；**断言以契约 §8.2 验收场景（Scenario）为准**——每个用例必须标注对应场景编号，一处定义、两处消费。

### 5.1 接口层用例（黑盒）

#### 用例 1：{用例名}（对应契约 §8.2 SC-1）

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1.1 | {操作} | {预期} |
| 1.2 | {操作} | {预期} |

**断言（可用 test.sh 实现）**

- **WHEN** {触发条件与动作}
- **THEN** {可观测结果：库字段 / 接口响应 / 待办变化}

#### 用例 2：{用例名}（对应契约 §8.2 SC-2）

| 步骤 | 操作 | 预期 |
|------|------|------|
| 2.1 | {操作} | {预期} |

**断言（可用 test.sh 实现）**

- **WHEN** {触发条件与动作}
- **THEN** {可观测结果}

> 删除功能不纳入自动测试（脚本只创建不删除），由人工界面验证。

### 5.2 库层断言（白盒，适用时必备）

| # | 断言 | 验证 SQL |
|---|------|----------|
| 1 | {主表状态/流程实例反写/软删除/乐观锁} | {见 §六 SQL 模板} |

### 5.3 编排层用例（跨服务流程，如适用）

| 用例 | 步骤 | 断言 |
|------|------|------|
| 聚合一致性 | {主责服务写入 → 调聚合层接口} | {聚合结果与主责服务状态一致（轮询等待至超时上限）} |

### 5.4 端层冒烟 Checklist（每端一份，人工执行留痕）

| 应用 | 关键路径 | 结果 |
|------|----------|------|
| {app-web} | {登录 → 列表 → 审批弹窗；权限按钮显隐} | {PASS/FAIL + 留痕} |
| {app-mobile} | {登录 → 提交 → 待办卡片；上传；返回键} | {PASS/FAIL + 留痕} |

## 六、验证 SQL

```sql
-- 主表状态与流程实例反写（{服务}库，按档案应用清单的数据库映射执行）
SELECT id, status, process_instance_id, del_flag
FROM {业务主表} WHERE {过滤字段} = {过滤值} ORDER BY create_time DESC LIMIT 10;

-- 流程运行时/历史（如适用）
SELECT * FROM {流程引擎库}.ACT_RU_TASK WHERE PROC_DEF_ID_ LIKE '{流程key}:%';
```

## 七、执行记录（只留最近 5 次）

| 日期 | 执行方式 | 结果（PASS/FAIL/跳过） | 遗留问题 |
|------|----------|------------------------|----------|
| {YYYY-MM-DD} | 脚本/手动 | {PASS} | {无} |
