# {模块名}接口文档

> 定位：本模块**字段级调用细节的唯一事实源**（供联调、测试、API 工具消费）。
> 分工：契约 §4 只保留"接口概览表 + 权限校验规则 + 接口对状态的影响"；字段细节以本文件为准；两处由 /文档 与 /代码 协同维护、/检查 校验一致。
> 全局约定（REST 风格/错误码/分页/认证）：见 `{文档根}/接口规范.md`。
> 导航区：
> 回链：本模块 契约.md / 测试.md / test.sh
> 关联文档：接口规范.md、总契约.md

---

## 1. 接口清单

| # | 方法 | 路径 | 权限标识 | 说明 |
|---|------|------|----------|------|
| 1 | GET | `/{资源}` | `{域}:{实体}:{操作}` | {说明} |
| 2 | POST | `/{资源}/{动作}` | ... | ... |

> 与契约 §4.2 概览表保持一致；新增/修改接口时两处同步。

## 2. 请求 / 响应定义

### 2.1 {接口名}

**请求**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| {field} | {type} | 是/否 | {说明} |

**请求示例**

```json
{
  "{field}": "{value}"
}
```

**响应**

| 字段 | 类型 | 说明 |
|------|------|------|
| {field} | {type} | {说明} |

**响应示例**

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": { }
}
```

## 3. 错误码

| 错误码 | 场景 | 提示文案 |
|--------|------|----------|
| {code} | {场景} | {文案} |

## 4. 分页参数

- 请求：`pageNum`（从 1 起）/ `pageSize`；
- 响应：`data.rows` + `data.total`。
