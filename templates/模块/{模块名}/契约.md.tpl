# {{模块名}}模块契约

<!-- doc-framework:docstruct {{框架版本}} | 全局文档结构版本；初始化渲染时替换为当时的框架版本（仅一个来源：package.json），框架升级后请人工对齐，见框架仓库 docs/设计文档.md §9.3 -->

> 所属业务域：{{业务域}}
> 模块形态：{{模块形态}}【如 简单 CRUD / 单级审批流 / 多级协同流程，按档案形态清单】
> **主责服务：{{主责服务}}**（本模块业务真相所在的服务端应用；聚合层仅透传/组合，不持有业务真相）
> 最后更新：{{YYYY-MM-DD}}
> 导航区（生成时自动填值）：
> 关联文档：总契约.md、`{{边界文档路径}}`【如 `边界/工作流边界.md`，无则删本项】、`规范/应用-{{涉及应用标识}}.md`（本模块落点涉及的应用规范，多份时逐一列出）
> 关联代码：{{标杆模块}} → {{标杆代码路径}}
> 回链：本模块 接口.md / 测试.md / test.sh（四件套互链）

> **记法**：`{{字段}}` = 待填；`[…]` = 示例/可选项（填好后连方括号删除）；`{记法}` = 路径与命名记法（如 `{模块名}`、`{域}:{实体}:{操作}`），照原样保留。

---

## 1. 模块定位

{{模块定位}}（一句话说明 + 解决的核心问题 + 项目级/平台级）

## 2. 边界约定

### 2.1 模块依赖关系

> 上游/下游与 总契约.md §5.1 依赖矩阵一致，变更时同步回写总契约（矩阵 + 全景图）。

```mermaid
flowchart TB
    subgraph UPSTREAM["上游依赖"]
        U["{{上游依赖}}"]
    end
    subgraph SELF["{{模块名}}"]
        S["{{本模块组件}}"]
    end
    subgraph DOWNSTREAM["下游/未来依赖"]
        D["{{下游依赖}}"]
    end
    U -->|"{{数据流}}"| S
    S -->|"{{数据流}}"| D
```

### 2.2 不做什么

- {{不做的边界1}}；
- {{不做的边界2}}。

### 2.3 业务流程（如适用）

> 跨模块流程的主图一律在 总契约.md §6 登记（编号 BF-xx）；本节只画**完全在本模块内部**的子流程并回链总契约编号，禁止重复绘制同一流程。

- 所属总契约流程：{{BF编号}}【如 `BF-01`；无则填 `无`】；
- 本模块内部子流程：

```mermaid
flowchart TB
    A["{{内部步骤}}"] --> B["{{内部步骤}}"]
```

## 3. 数据模型

### 3.1 主表：{{表名}}

| 字段 | 类型 | 说明 |
|------|------|------|
| {{字段名}} | bigint | 主键（{{主键策略}}） |
| ... | ... | ... |

> 索引、软删除（del_flag）、乐观锁（version）按档案通用约定（应用级覆盖见 `规范/应用-{{主责服务}}.md`）。

### 3.2 流程状态机（如适用）

> **跨应用不变量**：状态、流转、驳回语义全端一致，禁止任何端/服务自定义。

```mermaid
stateDiagram-v2
    [*] --> {{初始状态}}
    {{状态A}} --> {{状态B}}: {{触发动作}}
    {{状态B}} --> {{状态A}}: {{驳回动作}}
    {{状态B}} --> [*]: {{终态}}
```

| code | 枚举 | 说明 |
|:----:|------|------|
| {{code}} | {{枚举名}} | {{说明}} |

### 3.3 其他枚举（审核结果等）

| code | 枚举 | 说明 |
|:----:|------|------|
| {{code}} | {{枚举名}} | 不通过 |
| {{code}} | {{枚举名}} | 通过 |

## 4. 接口概览

> **字段级细节一律见 接口.md（唯一事实源）**；本节只列概览、权限校验规则与接口对状态的影响。
> **白名单（本节只允许出现这四类内容）**：4.1 基础路径 + 4.2 接口清单（方法 / 路径 / 权限标识 / 说明）+ 4.3 权限校验规则 + 4.4 接口对状态的影响。
> **越界判据（/module-review 按此判 HIGH）**：本节出现**字段名 / 字段类型 / 长度 / 必填性 / 请求或响应示例 / 错误码明细 / JSON 结构**，即为越界——这些一律改放 `接口.md`（防双维护）。
> 三向对账：本节"接口对状态的影响"须能与 总契约.md §6 业务流程、本契约 §3.2 状态机互相印证。

### 4.1 基础路径

```
/{{资源路径}}（服务归属：{{主责服务}}）
```

### 4.2 接口清单

| 方法 | 路径 | 权限标识 | 说明 |
|------|------|----------|------|
| GET | `/list` | `{域}:{实体}:list` | 分页列表 |
| ... | ... | ... | ... |

### 4.3 权限校验规则（{{权限模型}}，【如 岗位优先 + 单位/关系兜底】）

| 操作 | {{优先校验}} | {{兜底校验}} |
|------|--------------|--------------|
| {{操作}} | {{校验值}} | {{兜底值}} |

### 4.4 接口对状态的影响

- {{接口A}}：{{状态变化}}；
- {{接口B}}：{{状态不变，仅写业务字段}}。

## 5. 应用落点

> 本模块（这份业务语义）**在哪些应用中存在、各落在哪**。本表登记的是**已生效真相**（应然 ≡ 实然——契约 = 当前代码的语义映射），/module-review 负责对账。
> 纪律：**落点先登记后实施**——扩展到新应用先把落点写进计划的「语义增量」节（ADDED 行，目标位置 = 契约 §5），实施后由 `/module-doc` M 合并进本表，再写代码。
> **本表还是 /module-code 直改通道的边界来源**（免计划小改的应用级白名单）：落点多了 → 边界过宽，落点少了 → 直改被误判越界，所以必须如实完整。
> 单应用模块只保留一行；页面结构图每端一张 mermaid，端多时折叠为链接。

| 应用 | 页面结构 | 文件位置 | 权限控制方式 | 差异说明 |
|------|----------|----------|--------------|----------|
| {{主责服务}}（主责） | — | `{{主责任务落点文件}}` | {{权限控制方式}} | 业务真相 |
| {{落点应用}} | {{页面结构}}【如「列表页 + 审批弹窗」；服务端应用画 `—`】 | `{{落点文件}}`【如 `views/order/…`】 | {{权限控制方式}}【如 `v-hasPermi` 指令 / 网关鉴权 / 注解】 | {{差异说明}} |

> 每多一个落点应用**增一行**。各端页面结构（每端一张 mermaid；端多时折叠为链接）：

```mermaid
flowchart LR
    L["列表页"] --> D["{{弹窗 / 上传 / 下载}}"]
```

## 6. 标杆参考

- 与标准 CRUD 的差异：{{与标杆的差异说明}}。

| 参考点 | 应用 | 标杆模块 | 代码路径 |
|--------|------|----------|----------|
| {{参考点}} | {{应用标识}} | {{标杆模块}} | `{{标杆代码路径}}` |

## 7. 特殊约定与陷阱

1. {{陷阱或约定1}}；
2. {{陷阱或约定2}}。

## 8. 测试与验证

### 8.1 测试文档

- 测试文档：`{文档根}/模块/{{模块名}}/测试.md`；
- 测试脚本：`{文档根}/模块/{{模块名}}/test.sh`。

### 8.2 验收标准

| # | 验收项 | 预期结果 | 对应场景 |
|---|--------|----------|----------|
| 1 | {{验收项}} | {{预期结果}} | SC-1 |

> 写法：`#### Scenario: SC-n {短名}` + `- **WHEN** …` / `- **THEN** …`；THEN 必须写**可观测结果**（库字段值 / 接口响应 / 页面表现），不写"功能正常"这类不可观测的话。

#### Scenario: SC-1 {{场景短名}}

- **WHEN** {{触发条件与动作}}【如：填报账号提交订单】
- **THEN** {{可观测结果}}【如：主表 `status=待审核` 且 `process_instance_id` 非空】

#### Scenario: SC-2 {{场景短名}}

- **WHEN** {{触发条件与动作}}
- **THEN** {{可观测结果}}

## 9. 变更记录

> **一条一行**：结论 + 理由一句话 + **回链凭据**。实施细节（类名/方法/行号/SQL 片段/取证过程）留在提交信息与计划里，**不抄进本表**（`check` 对单行 >300 字符给 ℹ️）。
> **回链凭据**必须"不会消失"：`计划标识 @提交SHA` / `@提交SHA` / `需求#编号`；指向计划文件路径的旧写法仍被识别，但计划被清理后只给 ℹ️。
> `合并` / `实现` 行的**凭据为空 → `check` 硬报错**。

| 日期 | 来源 | 结论与理由 | 回链凭据 |
|------|------|------------|----------|
| {{YYYY-MM-DD}} | {{需求 / 代码}} | {{提炼或补建的结论一句话}} | {{凭据}} |
| {{YYYY-MM-DD}} | 合并 | {{增量一句话摘要}} | {{计划标识 @提交SHA}} |
| {{YYYY-MM-DD}} | 实现 | {{小改结论一句话}}（无语义变更） | {{@提交SHA}} |

## 10. 生产环境 SQL 执行登记

> **任何需人工在生产环境执行的 SQL**（建表/加字段/初始化数据/菜单权限等）都在此登记：只记引用，不复制 SQL 正文；执行后回填状态与日期。
> 只写在实施计划里不算——计划归档后即失效（§9 才是长期真相）。

| 日期 | 所属服务 | 文件 | 影响摘要 | 状态 |
|------|----------|------|----------|------|
| {{YYYY-MM-DD}} | {{所属服务}} | `{服务代码根}/sql/change/YYYY-MM-DD_{模块}_{说明}.sql` | {{影响摘要}} | {{待执行 / 已执行}} |

> - **只记引用不抄正文**：SQL 正文留在 `{服务代码根}/sql/change/YYYY-MM-DD-{模块}_{说明}.sql`（唯一真相是那个文件本身）；
> - **状态回填**：生产发布执行后回填"已执行（{{YYYY-MM-DD}}）"；/module-review 查账时校验漏登与状态是否属实；
> - 无生产 SQL 时**保留空表**（不要删本节——它是查账的锚点）。
