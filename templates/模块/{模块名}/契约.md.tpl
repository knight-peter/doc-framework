# {模块名}模块契约

> 所属业务域：{业务域}
> 模块形态：{简单 CRUD / 单级审批流 / 多级协同流程 / ...（按档案形态清单）}
> 最后更新：{YYYY-MM-DD}
> 导航区（生成时自动填值）：
> 关联文档：总契约.md、{边界/能力边界.md}、{规范/后端开发规范.md}
> 关联代码：{标杆模块} → {标杆代码路径}
> 回链：本模块 接口.md / 测试.md / test.sh（四件套互链）

---

## 1. 模块定位

{一句话说明 + 解决的核心问题 + 项目级/平台级}

## 2. 边界约定

### 2.1 模块依赖关系

> 上游/下游与 总契约.md §4.1 依赖矩阵一致，变更时同步回写总契约（矩阵 + 全景图）。

```mermaid
flowchart TB
    subgraph UPSTREAM["上游依赖"]
        {依赖实体/服务}
    end
    subgraph SELF["{模块名}"]
        {自身组件}
    end
    subgraph DOWNSTREAM["下游/未来依赖"]
        {下游}
    end
    {上游} -->|{数据流}| SELF
    SELF -->|{数据流}| {下游}
```

### 2.2 不做什么

- {不做的边界 1}；
- {不做的边界 2}。

### 2.3 业务流程（如适用）

> 跨模块流程的主图一律在 总契约.md §五 登记（编号 BF-xx）；本节只画**完全在本模块内部**的子流程并回链总契约编号，禁止重复绘制同一流程。

- 所属总契约流程：{BF-xx 或 "无"}；
- 本模块内部子流程：

```mermaid
flowchart TB
    {内部步骤}
```

## 3. 数据模型

### 3.1 主表：{表名}

| 字段 | 类型 | 说明 |
|------|------|------|
| {id} | bigint | 主键（{主键策略}） |
| ... | ... | ... |

> 索引、软删除（del_flag）、乐观锁（version）按档案通用约定。

### 3.2 流程状态机（如适用）

```mermaid
stateDiagram-v2
    [*] --> {初始状态}
    {状态A} --> {状态B}: {触发动作}
    {状态B} --> {状态A}: {驳回}
    {状态B} --> [*]: {终态}
```

| code | 枚举 | 说明 |
|:----:|------|------|
| {0} | {PENDING_XXX} | {说明} |

### 3.3 其他枚举（审核结果等）

| code | 枚举 | 说明 |
|:----:|------|------|
| {0} | {REJECT} | 不通过 |
| {1} | {PASS} | 通过 |

## 4. 接口概览

> **字段级细节一律见 接口.md（唯一事实源）**；本节只列概览、权限校验规则与接口对状态的影响。
> 三向对账：本节"接口对状态的影响"须能与 总契约.md §五 业务流程、§3.2 状态机互相印证。

### 4.1 基础路径

```
/{资源路径}
```

### 4.2 接口清单

| 方法 | 路径 | 权限标识 | 说明 |
|------|------|----------|------|
| GET | `/list` | {域}:{实体}:list | 分页列表 |
| ... | ... | ... | ... |

### 4.3 权限校验规则（{权限模型}，如 岗位优先 + 单位/关系兜底）

| 操作 | {优先校验} | {兜底校验} |
|------|------------|------------|
| {操作} | {校验值} | {兜底值} |

### 4.4 接口对状态的影响

- {接口 A}：{状态变化}；
- {接口 B}：{状态不变，仅写业务字段}。

## 5. 前端模式

```mermaid
flowchart TB
    {页面结构：页面 → 弹窗/上传/下载}
```

- 文件位置：{api 文件}、{views 文件}、{组件文件}；
- 按钮显隐：`v-hasPermi` + 计算属性（按状态 + 角色）；
- 与标准 CRUD 的差异：{差异说明}。

## 6. 标杆参考

| 参考点 | 标杆模块 | 代码路径 |
|--------|----------|----------|
| {参考点} | {标杆} | {路径} |

## 7. 特殊约定与常见陷阱

1. {陷阱/约定 1}；
2. {陷阱/约定 2}。

## 8. 测试与验证

- 测试文档：`{文档根}/模块/{模块名}/测试.md`；
- 测试脚本：`{文档根}/模块/{模块名}/test.sh`；
- 验收标准清单：

| # | 验收项 | 预期结果 |
|---|--------|----------|
| 1 | {验收项} | {预期} |

## 9. 变更记录

> 变更内容如含需人工在生产环境执行的 SQL（建表/加字段/初始化数据/菜单权限等），**同步在 §10 登记**，勿只写在本节。

| 日期 | 变更内容 |
|------|----------|
| {YYYY-MM-DD} | 新建（或：{变更内容}） |

## 10. 生产环境 SQL 执行登记（防上线遗漏）

> 登记**必须人工在生产环境执行的 SQL**（建表/加字段/初始化数据/菜单权限等），发布执行与查账以此为凭；纯开发/测试、不落生产的脚本不登记。
> 纪律：
> - **只记引用不抄正文**：SQL 正文留在 `sql/change/YYYY-MM-DD-{模块}_{说明}.sql`（唯一事实源），本节只登记文件引用 + 影响摘要，禁止在契约内复制 SQL 全文；
> - **登记义务**：/module-code 实施含需生产执行 SQL 的计划时，回写阶段在本节追加一行（状态=待执行）；实施计划归档后本节仍是长期登记（唯一真相）；
> - **状态回填**：生产发布执行后回填"已执行（{YYYY-MM-DD}）"；/module-review 查账时校验漏登与状态属实。

| 关联计划 | SQL 文件 | 影响摘要 | 执行状态 |
|----------|----------|----------|----------|
| {YYYY-MM-DD-{主题}实施.md} | `sql/change/YYYY-MM-DD_{模块}_{说明}.sql` | {建表 / 加字段 / 初始化数据} | 待执行 / 已执行（{YYYY-MM-DD}） |
