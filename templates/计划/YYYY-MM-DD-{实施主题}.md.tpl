# YYYY-MM-DD {实施主题}实施

> 依据模块契约：`{文档根}/模块/{模块名}/契约.md`
> 计划形态：完整（**轻量**形态只保留 §二逐应用表态 / §三变更文件清单 / §四任务清单 三节，其余节删除）
> 状态：{待审核 | 修订中 | 已批准 | 实施中 | 已完成 | 已废弃}（审核通过后开始编码）
> 参考标杆：{标杆模块}（{代码路径}）

> **依据填写规则**：
> - **存量模块**（有契约）：填上面的「依据模块契约」（跨模块计划若依赖未建档模块，可指向该模块的首次建模计划路径）；
> - **待实现模块**（代码库零实现、无契约）：删除「依据模块契约」行，改填 `> 依据探索记录：\`{文档根}/探索/YYYY-MM-DD-{主题}.md\``（**必填**；人工显式降级时才写 `无（自述）`，AI 不得自行省略）；
> - **首次建模计划必须用「完整」形态**，且必须含下面的「语义增量」节与「建模补充」节——轻量形态不承载语义，会产出"零信息建档"。

> **形态判据**（与 /module-code 三通道一致）：触及契约语义 / 含 SQL / 跨模块 / 需留设计决策 → 完整；
> 单应用小改、不触语义、无 SQL、无需决策 → 轻量（`check` 对轻量计划要求表态+清单齐全）。

---

## 〇、修订记录

| 版本 | 日期 | 状态 | 修改点 | 评审结论 |
|------|------|------|--------|----------|
| v1 | {YYYY-MM-DD} | 待审核 | 初稿 | — |

## 一、需求理解

{需求背景与目标描述}

## 二、逐应用表态（授权作用域）

> **红线**：档案应用清单里每个受影响的应用必须显式标注 `改动 / 本次不改 / 不适用`，漏表态 = 漏改。
> 本表 + §三变更文件清单共同构成 /module-code 的**白名单（= 授权作用域）**：
> 允许改动的路径 = 表态"改动"应用的代码根 ∩ §三列出的文件；越界即停，回 /module-plan 修订。
> **作用域不可就地改**（须退回修订中重新批准）；**作用域内追加任务项属记账**，可直接更新。

| 应用 | 表态 | 说明 |
|------|------|------|
| {svc-main} | 改动 / 本次不改 / 不适用 | {说明} |
| {svc-bff} | 改动 / 本次不改 / 不适用 | {说明} |
| {app-web} | 改动 / 本次不改 / 不适用 | {说明} |
| {app-mobile} | 改动 / 本次不改 / 不适用 | {说明} |

## 语义增量（delta）——实施后由 /module-doc 合并进契约与接口.md

> 合并状态：待合并
> 纪律：① 只写**目标语义**（不写实现步骤）；② 每条指明目标位置，可被机械合并；
> ③ 一份语义变更只由一份计划承载——拆分计划在「主计划」列回指主计划（同模块两份都为空且增量非空 → `check` 报错）；
> ④ 实施后由 `/module-doc` 合并并把本行翻成 `已合并`；实施中偏离计划 → **以代码为准**，并在契约 §9 记「实施偏离」；
> ⑤ **纯实现改动**（不触语义）写"无（纯实现改动）"，并把本行标为 `无需合并`；
> ⑥ 轻量形态计划**删除本节**（轻量不承载语义；发现触语义 → 升级完整计划）。

### ADDED（新增）

| # | 目标位置 | 对象 | 内容 | 主计划 |
|---|----------|------|------|--------|
| A1 | 契约 §{x.y} {表/章节} | {字段/状态/接口/落点/场景} | {目标语义} | |

### MODIFIED（修改）

| # | 目标位置 | 对象 | 现值 | 目标值 | 主计划 |
|---|----------|------|------|--------|--------|
| M1 | 契约 §{x.y} {表/章节} | {对象} | {现值} | {目标值} | |

### REMOVED（删除）

| # | 目标位置 | 对象 | 理由 | 主计划 |
|---|----------|------|------|--------|
| R1 | 契约 §{x.y} {表/章节} | {对象} | {理由} | |

## 建模补充（供 /module-doc 建档：create 分支）

> **待实现模块（首次建模计划）必填**；存量模块的常规计划整节删除。
> 语义增量的三张表装不下图与边界约定，建档时（M 的 create 分支）依赖本节生成契约 §1/§2/§6/§8。

### M1 边界与「不做什么」

{本模块管什么、不管什么；与相邻模块的边界}

### M2 状态机 / 页面结构（mermaid）

```mermaid
stateDiagram-v2
    [*] --> {初始状态}
    {初始状态} --> {下一状态}: {动作}
```

### M3 依赖与集成点

{调用谁 / 被谁调用 / 共享的状态机或权限域；跨模块接口的字段与语义（此时该模块尚无契约，接口语义以此为准）}

### M4 关键验收 Scenario（映射到契约 §8.2）

| # | 场景 | 前置 | 动作 | 期望 |
|---|------|------|------|------|
| SC-1 | {场景名} | {前置} | {动作} | {期望} |

## 三、变更文件清单

### 1) SQL（按服务分目录）

| 文件 | 所属服务 | 说明 |
|------|----------|------|
| {服务代码根}/sql/change/YYYY-MM-DD_{模块}_{说明}.sql | {应用标识} | {说明} |

### 2) 服务端应用

| 应用 | 文件 | 说明 |
|------|------|------|
| {svc-main} | {文件路径} | {说明} |
| {svc-bff} | {文件路径} | {说明} |

### 3) 前端端应用

| 应用 | 文件 | 说明 |
|------|------|------|
| {app-web} | {文件路径} | {说明} |
| {app-mobile} | {文件路径} | {说明} |

### 4) 其他（工作流/边界/文档）

| 文件 | 说明 |
|------|------|
| {文件路径} | {说明} |

## 四、任务清单（实施时逐项勾选）

> 本清单是 /module-code 的**执行序列与进度事实源**：实施过程中逐项勾选，`/module-review` 按此核对完成度。
> 写法：`- [ ] 组.序号 任务`（组对应 §五实施顺序阶段）；任务须**可独立验证**（能对应一个文件、一个接口或一次断言）。
> 纪律：**只勾选事实完成的项**（禁止提前勾选）；存在未勾选项时，计划状态不得登记「已完成」。

- [ ] 1.1 {任务：如 执行 DDL 变更脚本至开发/测试库}
- [ ] 1.2 {任务}
- [ ] 2.1 {任务：如 主责服务实体/Mapper/Service/Controller}
- [ ] 2.2 {任务}
- [ ] 3.1 {任务：如 聚合层接口按接口.md 实现}
- [ ] 4.1 {任务：如 web 端列表页与审批弹窗}
- [ ] 4.2 {任务：如移动端提交页与待办卡片流}
- [ ] 5.1 {任务：如 接口层 + 库层 + 编排层 + 端层验证}

## 五、实施顺序（从应用拓扑推导依赖序）

1. SQL（各服务 `sql/change/`，开发/测试环境执行）；
2. 主责服务（接口语义已在「语义增量」节定稿；接口.md 的字段级真相随实施回写 `/module-doc`）；
3. 下游/聚合服务（按已定契约实现转发/聚合）；
4. 逐端实施（每端独立自检）；
5. 测试与回写（按 §四任务清单 5.x 与 §十一回写清单执行）。

## 六、关键设计决策

1. {决策 1}：{说明}；
2. {决策 2}：{说明}。

## 七、接口兼容性声明（涉及接口变更时必填）

- 变更类型：{兼容（新增字段/新增接口） | 破坏性（删/改字段、改路径、改语义）}；
- 破坏性变更必须：所有消费方在 §二表态「改动」，并在本计划内同步完成。

## 八、验收标准（对应契约 §8）

| # | 验收项 | 对应契约场景 |
|---|--------|--------------|
| 1 | {验收项} | {§8.2 Scenario: {场景名}} |
| 2 | {验收项} | {§8.2 Scenario: {场景名}} |

## 九、待确认事项

1. {待确认项}（已确认/待确认）。

## 十、里程碑

1. 本计划审核通过 → 部署 SQL（开发/测试环境执行脚本）→ 按 §五依赖序实施 → 注册/部署流程（如适用）→ 回写（含生产 SQL 登记到契约 §10）→ /module-review。

## 十一、回写清单（实施完成后逐项执行）

- [ ] §四 任务清单全部勾选（存在未勾选项时不得登记「已完成」）
- [ ] 契约变更记录追加一行（{模块名}/契约.md §9），并**标注来源**（需求 / 代码 / 实现 / 合并）
- [ ] 「语义增量」的 `合并状态` 翻成 `已合并`（由 `/module-doc` 合并后翻；空增量标 `无需合并`）
- [ ] **首次建模计划专用**：由 `/module-doc` M 的 `create` 分支建档（契约 10 章 + 接口.md），并回写总契约（模块索引表 / 依赖矩阵 / 应用拓扑）
- [ ] 含需生产执行的 SQL 时，契约 §10 生产环境 SQL 执行登记追加一行（状态=待执行）
- [ ] 接口.md 同步（字段级变化；由 `/module-doc {模块名}` 处理）
- [ ] 接口.md §5 各应用接入状态更新（本次涉及的应用打勾）
- [ ] 契约 §5 应用落点表同步（新增落点/差异说明变化时）
- [ ] 总契约模块索引表更新（由 /module-doc 维护）
- [ ] 测试执行记录登记（{模块名}/测试.md）
- [ ] 计划状态登记「已完成」后，提示 `/module-plan {模块名} 归档`（移入 `计划/归档/`）
