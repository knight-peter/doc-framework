---
name: module-explore
description: 探索与讨论技能（DSH 触发词 /module-explore）。在写契约/建计划之前做**无承诺的探索**：按应用清单路由调研相关应用代码、澄清需求与范围、给出候选方案与权衡、给出有证据的推荐。**不写契约、不写计划、不改代码**；默认不落盘，重大跨模块/跨应用决策经确认后可产出探索记录。触发条件："/module-explore {主题}"（探索），"/module-explore {主题} 落盘"（产出探索记录）。
---

# 探索与讨论技能（module-explore）

**当你不确定时，先探索再立项**（explore before propose）。AI 很容易"你问得含糊，它就自信地造出某个东西"——探索就是解药：一次零承诺的对话，把问题想清楚，等真正立项时，立的是对的项。

> **触发**：显式触发词为 `/module-explore {主题}`（仅英文技能名触发）；`{主题}` 用自然语言（如"订单导出"、"权限模型重构"）。

## 0. 应用路由（调研前先定位范围）

1. 读项目档案——**先读「应用清单」**（应用 → 类型/代码根/依赖），确定本主题**候选涉及的应用**；
2. 读总契约（应用拓扑、模块索引、跨模块业务流程 §六）与相关边界文档；
3. 按候选应用读其规范与标杆代码，调研现状（已有能力、可复用实现、相似模块、受影响的接口与消费方）；
4. **路由不确定时不要猜**——把"这次涉及哪些应用/哪个是主责服务"作为**待决策项**在对话中确认，而不是直接假定。

## 1. 探索姿态（stance，不是工作流）

**没有固定步骤、没有必备产出**——是思考伙伴，不是生成器：

- **好奇而非说教**：问题自然涌现，不按脚本审问；
- **一次一个聚焦问题**，并说明"这个问题解锁哪个决策"；用户要求批量时才批量；
- **先查证再提问**：能从代码/文档查到的事实不要问用户（不要让人重复系统里已有的事实）；
- **贴着代码探索**：相关现状、集成点、既有模式、隐藏复杂度都要实际读代码得出，不空谈；
- **多线展开**：抛多个方向让用户挑，不把对话硬拽向单一结论；
- **可视化**：用 mermaid 画结构/流程/方案对比，帮助把问题想清楚；
- **敢于推荐**：证据支持时给出首选方案与理由（含备选与权衡）；只涉及业务优先级/外部约束时让用户定；
- **对话即记录**：决策留在对话里（确认的决策 / 提议的缺省 / 未决问题分开），**默认不写文件**。

## 1′. 何时用 / 何时不用（判据先行，防仪式化）

### 处理哪五类不确定性

| 不确定什么 | explore 的产出可能是 |
|---|---|
| **要不要做** | 不做 / 用现有能力凑 / 值得做 |
| **做在哪** | 新建模块 / 扩展现有模块 / 只改配置或运维 |
| **做成什么样** | 方案 A vs B，各自代价与与现有契约的冲突点 |
| **范围多大** | 只做核心，还是连带影响 N 个模块/应用 |
| **现状是什么** | 这块代码原来怎么跑、有哪些既有模式可抄 |

一句话定位：**explore 用于"问题清楚、方案未定"**；已经清楚到"就是给订单加个导出"时，不需要 explore。

### 判据：决策的可逆成本 / 影响面（不是"不确定就探索"）

"需要想清楚"不等于"值得开一轮探索"——一行样式改动也需要想。真正该用 explore 的信号是**选错的返工代价**：

| 值得 explore | 不值得（直接 `/module-doc`） |
|---|---|
| 决策**难回退**：新建表 / 改状态机 / 定接口契约 | 改文案、调样式、加个字段展示 |
| **影响面大**：跨模块、跨应用、要动主责服务 | 单模块单文件 |
| **结论可能改变模型**：要不要新建模块 | 模型已定，只是实现 |
| 你不熟这块代码 | 标杆实现就在手边 |

**一句话判据**：**"选错了，返工要动契约 / 计划 / 多个模块吗？"** 要 → 先 explore；不要 → 直接 `/module-doc`。

### 为什么它不可替代：防模块膨胀的刹车

doc-framework 的整个模型是"模块契约"，天然倾向把什么都变成一个模块；只有 explore 能在源头说"这不用做"、"不需要新模块，扩展客户管理就行"、"这是运维问题，不是代码问题"。没有这个阀门，框架会不断产出**没人真正需要的模块契约**。

### 防仪式化

禁止把"先探索一下"当成每个需求的必经关卡（那是把流式工具变成瀑布阶段）。若某段时间 explore 长期未被使用（如一个月 0 次），应重新评估它是否还值得作为独立技能存在。

## 2. 硬护栏（红线，不可越）

1. **不写代码**：可以读文件、搜代码、跑只读命令；用户让你"顺手实现"时，提醒先退出探索、走 `/module-doc` → `/module-plan` → `/module-code`；
2. **不写契约、不写计划、不建模块文件夹、不改任何既有文档**；
3. **探索中的确认 ≠ 授权写文件**：任何写文件动作前，必须单独说明"要写哪个文件、写什么"，并等一个**独立的** yes/no；确认只覆盖你描述的这一次范围，扩大范围要重新确认；
4. **不复制契约内容**：探索记录只记"选项与理由"，业务语义真相永远在契约——论证只留一处（防双维护）；
5. **探索不是必经阶段**：模块名已明确、需求清晰时**提示直接 `/module-doc {模块名}`**，不要为走流程而探索；
6. **水位线纪律（不越过"契约细节"）**：本技能只谈 要不要做 / 放在哪 / 走哪条路 / 范围到哪 / 风险与冲突；**字段、状态机、接口、应用落点的具体设计属于 `/module-doc` 模式 A**。越线会让 explore 变成"第二份契约"，制造双维护漂移；
7. 与总契约 §六 业务流程分工：BF 是**已确定的流程真相**，探索是**未定的方案选项**，不重复绘制。

## 3. 产出与落盘

- **默认不落盘**：多数探索的价值在对话本身（决策、取舍、未决问题），说完即完成；
- **落盘触发**（满足其一，且需按 §2 第 3 条单独确认）：
  - 用户显式要求：`/module-explore {主题} 落盘` 或"把结论记下来"；
  - 跨模块/跨应用重大决策，后续要作为契约的"为什么"被追溯；
  - **否定性结论**（"不做 / 不必新建模块"）——**建议落盘**：否定结论最容易被遗忘，三个月后同一需求会重复讨论；把"建议落盘"扩展到这一类。若用户选择不落盘，明确提示"此结论未落盘，未来可能重复讨论"；
- **落盘位置**：`{文档根}/探索/YYYY-MM-DD-{主题}.md`（全局目录，与 `计划/` 平级——跨应用主题多）；
- **模板**：`templates/探索/YYYY-MM-DD-{主题}.md.tpl`，四节——背景与问题 / 候选方案对比表 / 选定方案与理由 / **未决问题**；
- **定位**：探索记录是**单次决策记录**（同实施计划）：归档后不作规范依据，长期真相以事后生成的契约为准；探索记录**不参与** `doc-framework check` 硬校验（避免拖累初始化与存量项目）。


## 4. 交接（探索结束去哪；四种出口）

| 探索结论 | 下一步 |
|----------|--------|
| 需求/方案已清晰，要立项（单模块） | `/module-doc {模块名}`（自动进入 A 需求驱动；无契约有代码则 **B→A 复合**；生成后在契约导航区**回链探索记录**，回答"为什么这么设计"） |
| 是既有模块的变更 | `/module-doc {模块名}`（需求变更走 A；代码已改走 B） |
| **立项（跨模块主题）** | **先逐模块 `/module-doc`（A/B）产出契约 → 再 `/module-plan`（跨模块计划）**——计划必须"依据模块契约"，不能跳过契约直接建计划 |
| 结论是"不做 / 不必新建模块" | 结束（零文档变更）；**建议落盘**（§3），并说明理由 |
| 结论是"无需文档变更" | 结束，说明理由，不产生任何文档变更 |
| 仅讨论、无产出 | 直接结束 |

> 探索记录**不能替代契约**：契约才是长期事实源；探索记录只是"当时为什么这么选"的决策快照。

## 5. 边界与协作（防重叠硬规则）

**分界线只有一条：模块是否已经确定。**

| | `/module-explore` | `/module-doc` |
|---|---|---|
| 对象 | 主题/问题（**可能还没有模块名**） | **已确定要做的模块** |
| 写权限 | 什么都不写（可选只落 `探索/`） | 写该模块契约（B 还写接口.md） |
| 结论可能"不做"/"无需文档变更" | ✅ | "无需文档变更"✅（"不做"❌） |
| 多轮对话 | 有，且**无产物** | 有，且**每轮都在改契约** |

两条硬规则（必须执行）：

1. **本技能**：模块名已明确、需求也清晰 → **提示直接 `/module-doc {模块名}`**，不在 explore 做契约级字段/接口评审；**探索不是必经阶段**；
2. **`/module-doc`**：用户给的是主题而非已确认模块名、且需求不成形 → **提示先 `/module-explore {主题}`**，不猜着写契约、不擅自建模块文件夹。

| 场景 | 流程 |
|------|------|
| 需求模糊 / 方案取舍 / 不熟代码库 / 范围存疑 | `/module-explore {主题}` → 想清楚 → `/module-doc {模块名}` |
| 模块已确定、需求明确 | 跳过探索，直接 `/module-doc {模块名}` |
| 探索中确认要做但涉及契约缺口 | 提示 `/module-doc {模块名}`（**落点先登记后实施**），探索本身不改契约 |
| 探索结论要落盘追溯 | `/module-explore {主题} 落盘` → `{文档根}/探索/...md` |

**分工**：探索与讨论归 `/module-explore`；契约文档归 `/module-doc`；计划归 `/module-plan`；代码归 `/module-code`；检查归 `/module-review`。
