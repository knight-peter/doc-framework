---
name: module-review
description: 模块审查技能（DSH 触发词 /module-review）。对照模块四件套文档与各应用代码检查，输出分级问题清单（CRITICAL/HIGH/MEDIUM/LOW），含多应用对账（契约落点 ↔ 计划表态 ↔ 接口接入状态 ↔ git diff 实际变更）与回写查账。触发条件：用户输入"/module-review {模块名}"。
---

# 模块审查技能（module-review）

自动定位：读档案（**应用清单路由** + 注入检查项）→ 读总契约（索引、应用拓扑）→ 读 `{文档根}/模块/{模块名}/` 四件套（契约/接口/测试/脚本）→ 对照**落点涉及的各应用代码**检查。

> **触发**：显式触发词为 `/module-review {模块名}`（仅英文技能名触发）。

## 检查清单

### 一、通用清单（任何技术栈都查）

| # | 检查项 | 违规级别 |
|---|--------|---------|
| 1 | 契约↔代码文件一致性（契约声明的表/字段/状态机与代码、SQL 一致） | HIGH |
| 2 | 接口.md 与契约 §4 概览一致（接口清单与权限标识对齐） | HIGH |
| 3 | 导航区完整（四件套互链、关联文档存在、标杆代码路径有效）；**未生成的文件标"（待生成）"不算断链**（对应 /module-doc 模式 A 停在契约） | MEDIUM |
| 4 | **回写查账**（校验 /module-code 的回写义务已履行：契约变更记录有登记、接口.md 与代码一致、总契约索引已更新） | 漏写即 HIGH |
| 5 | 软删除 / 乐观锁实现（按档案通用约定 + 应用层覆盖声明） | HIGH |
| 6 | SQL 参数化（无 `${}` 拼接用户输入） | CRITICAL |
| 7 | 无硬编码密钥/密码/Token | CRITICAL |
| 8 | 输入校验（新增/修改接口参数有校验） | MEDIUM |
| 9 | 依赖注入存在性（注入的 Service/接口确实存在） | CRITICAL |
| 10 | **生产 SQL 登记完整**（变更含需人工在生产执行的 SQL 时，契约 §10 已登记且状态属实，SQL 文件存在于**所属服务**的 `sql/change/`） | 漏登即 HIGH |

### 二、多应用对账（应然 ↔ 实然）

| # | 检查项 | 违规级别 |
|---|--------|---------|
| 11 | **落点对账**：契约 §5 落点表声明的每个应用，代码落点真实存在；代码有落点但契约未登记 = 私建 | HIGH |
| 12 | **计划表态对账**：近期已完成计划的「逐应用表态」属实——表态「本次不改」的应用代码零变更（用 `git diff --name-only` 核对，或提示用户跑 `doc-framework diff-check`）；实际改动 ⊆ 计划变更文件清单 | HIGH |
| 13 | **接入状态对账**：接口.md §5 各应用接入状态与各应用代码事实一致（标 ✅ 的端确实在调用，标 ❌ 的确实未接） | MEDIUM |
| 14 | **跨服务契约一致性**：聚合层/消费方调用的下游接口路径、字段、错误码与主责服务接口.md（服务端真相）一致 | HIGH |
| 15 | **跨应用不变量**：状态机（状态/流转/驳回语义）、权限标识、枚举 code 在各应用间完全一致——任何一端自定义即违规 | CRITICAL |
| 16 | **应用清单健康**：契约主责服务、§5 落点应用、接口.md 服务归属都在档案应用清单内；各应用代码根存在且无互相嵌套 | HIGH |

### 三、完成度对账（Completeness：计划任务 ↔ 实际产出）

> 审查维度说明：一、三节管 **Correctness**（实现是否符合契约），本节管 **Completeness**（计划声称做完的，是否真的做完）——两者都过才算可提交。

| # | 检查项 | 违规级别 |
|---|--------|---------|
| 17 | **任务勾选属实**：计划「任务清单」逐项核对——已勾选但无对应实现（假勾选）、有实现却未勾选（漏勾）都要列出并纠正 | HIGH |
| 18 | **状态与进度一致**：存在未勾选任务却已登记「已完成」，或任务全勾但状态仍是「实施中」——两者都不一致 | HIGH |
| 19 | **验收场景可追溯**：计划 §八验收项引用了契约 §8.2 的场景编号（SC-n），且场景在契约中确实存在；测试.md 用例标注了对应场景 | MEDIUM |
| 20 | **归档纪律**：状态为「已完成/已废弃」的计划是否已移入 `计划/archive/`（长期滞留活跃目录导致在途列表失真） | LOW |

### 四、测试覆盖完整性（对照 测试规范 §1.1）

| # | 检查项 | 违规级别 |
|---|--------|---------|
| 21 | **黑盒底线**：测试.md 存在「适用层与依据」，且**接口层（黑盒）标注适用**并有用例 | 缺失即 HIGH |
| 22 | **白盒适用判定**：模块涉及持久化状态 / 流程实例反写 / 软删除 / 乐观锁之一时，库层（白盒）必须适用且有断言；不适用必须写原因（不可用"写起来麻烦"作理由） | 该做未做且无理由即 HIGH |
| 23 | **端层/编排层适用判定**：契约 §5 有前端端落点 → 端层适用；涉及 ≥2 个服务端应用 → 编排层适用；不适用写明原因 | 该做未做即 MEDIUM |

### 五、模式残留对账（对应 /module-doc 三模式）

| # | 检查项 | 违规级别 |
|---|--------|---------|
| 24 | **§9 来源标注**：契约 §9 每条**新增**记录标注了来源（`需求` / `代码`）；缺失 = 可能走了静默覆盖（历史记录不回填、不判级） | MEDIUM |
| 25 | **C 前置可验证**：C 模式产出的测试.md，其断言引用的接口/字段在契约中确实存在（不存在说明契约与代码/实现已漂移） | HIGH |
| 26 | **A 停止点未被违反**：契约 §9 无"契约定稿"行却已生成接口/测试，且无法说明理由 | MEDIUM |

### 六、档案注入清单（按应用读取）

按项目档案「技术栈特有检查项」**逐应用**执行（如 svc-main 的"继承统一 Controller 基类"、app-web 的"按钮权限指令控制"），只检查对应应用的代码。

## 输出格式

```
## {模块名} 代码审查报告

### CRITICAL（必须修复）
| # | 检查项 | 文件 | 问题描述 |

### HIGH（强烈建议修复）
| # | 检查项 | 文件 | 问题描述 |

### MEDIUM（建议修复）
| # | 检查项 | 文件 | 问题描述 |

### LOW（可选修复）
| # | 检查项 | 文件 | 问题描述 |

### 检查统计
- 总检查项: N / 通过: N / CRITICAL: N / HIGH: N / MEDIUM: N / LOW: N

### 完成度（Completeness，对应第三节）
- 计划: {YYYY-MM-DD-{主题}实施.md}（状态: {实施中/已完成}）
- 任务进度: x/y ｜ 未完成项: {编号 + 原因，或"无"}
- 勾选异常: {假勾选 / 漏勾 / 无}

### 测试覆盖（Completeness，对应第四节）
- 接口层（黑盒）: {适用/缺失} ｜ 库层（白盒）: {适用且有断言 / 不适用+原因 / 该做未做} ｜ 端层: {…} ｜ 编排层: {…}
```

全部检查完成后，如果 CRITICAL 和 HIGH 均为 0，提示用户可提交；否则列出问题并建议相应修复：
- 文档/接口不一致 → `/module-doc {模块名}`（自动判定：代码是既成事实走模式 B 对账）；
- 代码问题 → `/module-code`（按计划修复）；
- 测试覆盖缺失 → 先补契约 §8.2 场景，再走 `/module-doc {模块名} 补齐接口和测试`（模式 C）。

**另外**：若计划存在未勾选任务却已登记「已完成」，直接指出并要求按事实修正（回 `/module-plan` 修订状态，或 `/module-code` 补齐未完成项）——状态是事实登记，不允许"完成度不够但状态已完成"。
